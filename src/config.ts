/**
 * @deprecated Import from `./config/index` directly.
 * This re-export shim exists for backward compatibility during the refactor.
 */
export {
  DEFAULT_CONFIG,
  I18nSharpenConfigSchema,
  loadConfig
} from "./config/index"
