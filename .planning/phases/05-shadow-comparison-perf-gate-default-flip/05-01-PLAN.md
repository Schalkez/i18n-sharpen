---
phase: 05-shadow-comparison-perf-gate-default-flip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - package.json
  - tsconfig.json
  - tsconfig.scripts.json
  - tests/corpus/SOURCES.md
  - tests/corpus/js-ts/
  - tests/corpus/vue/
  - tests/corpus/svelte/
  - tests/corpus/astro/
autonomous: true
requirements: [SHADOW-02]
must_haves:
  truths:
    - "scratch/ is gitignored so derived reports and test temp dirs are never committed"
    - "scripts/*.ts are typechecked by pnpm typecheck (caught by tsc, not silently skipped)"
    - "tsx is installed as a devDependency so pnpm shadow / pnpm bench can run scripts/*.ts"
    - "tests/corpus/ holds one real OSS project per framework (JS/TS, Vue, Svelte, Astro), permissively licensed"
    - "SOURCES.md records upstream repo URL, exact commit SHA, files taken, and license for each corpus source"
    - "Each vendored file uses a plain t('key') call (not $_/$t store/global), so extraction actually fires"
  artifacts:
    - path: ".gitignore"
      provides: "scratch/ ignore rule"
      contains: "scratch/"
    - path: "package.json"
      provides: "tsx devDependency"
      contains: "\"tsx\""
    - path: "tsconfig.scripts.json"
      provides: "typecheck coverage for scripts/ with @/ alias preserved"
      contains: "scripts"
    - path: "tests/corpus/SOURCES.md"
      provides: "provenance manifest for all four vendored corpora"
      contains: "License"
  key_links:
    - from: "tsconfig.scripts.json"
      to: "scripts/"
      via: "include array covering scripts directory"
      pattern: "scripts"
    - from: "package.json typecheck script"
      to: "tsconfig.scripts.json"
      via: "tsc -p tsconfig.scripts.json --noEmit (or include extended)"
      pattern: "tsconfig.scripts|scripts"
---

<objective>
Lay the Wave-1 foundation that unblocks both gate scripts (shadow + bench) and the flip:
gitignore `scratch/`, add the `tsx` devDependency, bring `scripts/*.ts` under typecheck
(preserving the `@/` alias), and vendor one real OSS project per framework into
`tests/corpus/` with a `SOURCES.md` provenance manifest.

Purpose: The shadow harness and the benchmark both read `tests/corpus/`, write to
`scratch/`, and run as `tsx scripts/*.ts`. None of that works until this plan lands.
The corpus must be real `t("key")` usage per framework so the differential diff
(SHADOW-02) actually exercises key extraction rather than passing vacuously.
Output: gitignored scratch/, tsx devDep, scripts typecheck config, vendored corpus + SOURCES.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-shadow-comparison-perf-gate-default-flip/05-CONTEXT.md
@.planning/phases/05-shadow-comparison-perf-gate-default-flip/05-RESEARCH.md

<interfaces>
<!-- Key facts the executor needs. Extracted from codebase + RESEARCH.md. Use directly — no exploration. -->

Current .gitignore does NOT contain `scratch/`. The existing ast-shadow.test.ts writes
temp projects to `../../scratch/temp-ast-shadow-{random}/`, so scratch/ already exists at
runtime but is untracked — gitignore it before any gate script runs.

Current package.json devDependencies has NO `tsx`. Build tool is `tsup`, test runner is
`vitest`. ESM project (`"type": "module"`). Node ≥ 20.

Current tsconfig.json has `"include": ["src"]` ONLY — scripts/*.ts are NOT typechecked
by `pnpm typecheck` (which runs `tsc --noEmit`). The `@/*` path alias maps to `./src/*`
and MUST be preserved for any new scripts config.

Corpus selection (PINNED in RESEARCH.md "Corpus Selection" — copy verbatim):

| Framework | Repo | SHA (HEAD at research time 2026-06-02) | License | Files to vendor |
|-----------|------|-----------------------------------------|---------|-----------------|
| JS/TS  | nelsonlaidev/nelsonlai.dev          | 76f67d7f29716d185b4f43f3aa7c0cde57d64873 | MIT | src/app/[locale]/(main)/about/page.tsx, blog/page.tsx, (main)/page.tsx |
| Vue    | vuesion/vuesion                     | b6683b566600bc55494d854e7d08e01248df4d05 | MIT | src/pages/index.vue, src/pages/services/index.vue (PAGES ONLY) |
| Svelte | Scorpio3310/sveltekit-i18n-starter  | 20d7b9b2631f7be3a8b694dced2208df9c89d5f6 | MIT | src/components/Navbar.svelte, Footer.svelte, src/routes/[lang=lang]/+page.svelte |
| Astro  | Scorpio3310/astro-i18n-starter      | f591db710affde9c260c5abe52fa1e3262da82e0 | MIT | src/components/Header.astro, Footer.astro, Info.astro |

CRITICAL corpus constraints (RESEARCH Pitfall 1):
- Vendor ONLY files where the i18n call is a plain function call `t("key")` in script/frontmatter.
- For Vue: vendor src/pages/*.vue (use `const { t } = useI18n()` + `t("...")` in <script setup>),
  NOT component-level files like LandingPageHeroSection.vue which use `$t('key')` template global.
- For Svelte: this starter uses a plain `t` function (`import { t } from "$i18n/i18n"`), NOT the
  `$_("key")` reactive store — that is why it was chosen. Do not swap in a store-based project.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gitignore scratch/, add tsx devDep, bring scripts under typecheck</name>
  <files>.gitignore, package.json, tsconfig.scripts.json</files>
  <read_first>
    - .gitignore (current state — scratch/ is NOT present; confirm before editing)
    - package.json (current devDependencies + scripts; tsx is absent; build=tsup, test=vitest)
    - tsconfig.json (current `"include": ["src"]` and `@/*` -> `./src/*` paths; preserve the alias)
    - .planning/phases/05-shadow-comparison-perf-gate-default-flip/05-RESEARCH.md (Pitfall 4 scratch/, Pitfall 5 tsx, "Tsconfig note" section)
  </read_first>
  <action>
1. Add `scratch/` to `.gitignore`. Append a line `scratch/` under a comment line
   `# Phase 5 gate-script reports + test temp projects (derived, never committed)`.
   Do NOT remove or reorder existing entries.

2. Add `tsx` as a devDependency in package.json. Use `pnpm add -D tsx` (this updates
   package.json AND pnpm-lock.yaml together — do not hand-edit the version). tsx is a
   devDependency only (NEVER a runtime dependency / `dependencies` entry) — this honors
   the "zero new runtime deps" constraint (D-10).

3. Bring `scripts/*.ts` under typecheck. Create `tsconfig.scripts.json` (cleaner than
   polluting the main build's include) that extends the base config, preserves the `@/`
   alias, and includes the scripts directory:
   ```json
   {
     "extends": "./tsconfig.json",
     "compilerOptions": {
       "noEmit": true,
       "types": ["node"]
     },
     "include": ["scripts", "src"]
   }
   ```
   (Include `src` too so `@/` imports inside scripts resolve against real source during typecheck.)

4. Update the `typecheck` script in package.json so it typechecks scripts as well. Change
   `"typecheck": "tsc --noEmit"` to `"typecheck": "tsc --noEmit && tsc -p tsconfig.scripts.json --noEmit"`.
   This keeps `pnpm typecheck` (and therefore CI) green-or-red on scripts too. The base
   `tsc --noEmit` still runs first (src-only), then the scripts config runs.

Note: scripts/*.ts do not exist yet (created in Plan 02/03). With no scripts files present,
`tsc -p tsconfig.scripts.json --noEmit` typechecks only `src` and passes — that is fine for
this plan; the config is in place ready for Plan 02/03.
  </action>
  <verify>
    <automated>node -e "const g=require('fs').readFileSync('.gitignore','utf8'); const p=require('./package.json'); if(!g.includes('scratch/'))throw new Error('scratch/ missing from .gitignore'); if(!p.devDependencies.tsx)throw new Error('tsx missing from devDependencies'); if(!p.scripts.typecheck.includes('tsconfig.scripts.json'))throw new Error('typecheck does not reference tsconfig.scripts.json'); if(!require('fs').existsSync('tsconfig.scripts.json'))throw new Error('tsconfig.scripts.json missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains the line `scratch/`
    - `package.json` devDependencies contains `tsx`
    - `tsconfig.scripts.json` exists and its `include` covers `scripts`
    - `package.json` `scripts.typecheck` references `tsconfig.scripts.json`
    - `pnpm typecheck` exits 0 (scripts dir empty so far is fine; @/ alias resolves)
    - `tsx --version` runs (binary resolvable via `pnpm exec tsx --version`)
  </acceptance_criteria>
  <done>scratch/ gitignored; tsx installed as devDep; tsconfig.scripts.json created with @/ alias preserved; typecheck script extended; `pnpm typecheck` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor one real OSS corpus per framework + write SOURCES.md</name>
  <files>tests/corpus/SOURCES.md, tests/corpus/js-ts/, tests/corpus/vue/, tests/corpus/svelte/, tests/corpus/astro/</files>
  <read_first>
    - .planning/phases/05-shadow-comparison-perf-gate-default-flip/05-RESEARCH.md ("Corpus Selection" — the PINNED repo/SHA/license/files table; Pitfall 1 plain-t() constraint; Pitfall 6 i18nKey-Vue-only note)
    - .planning/phases/05-shadow-comparison-perf-gate-default-flip/05-CONTEXT.md (D-01 vendored, D-02 one-per-framework, D-03 SOURCES.md, D-04 permissive license)
    - src/__tests__/parsers/fixtures/ (existing fixtures — vue-setup.vue, vue-legacy.vue, component.svelte, page.astro — these JOIN the corpus in the diff; do not duplicate them)
  </read_first>
  <action>
Vendor a curated subset of real files per framework into `tests/corpus/`. Files are
COPIED INTO the repo and committed (D-01 — not submodules, not fetch-on-demand). The four
framework compilers (`@vue/compiler-sfc`, `svelte`, `@astrojs/compiler`, `typescript`) are
already in devDeps so all vendored files parse without extra setup.

SECURITY NOTE: these vendored corpus files are third-party source COPIED into the repo. They
are PARSED by shadow/bench (via the workspace compilers), NEVER executed, and MUST NOT be
wired into the build (`tsup`) or test (`vitest`) graph as importable modules. Keep them
strictly under `tests/corpus/` and reference them only as file paths read by the gate scripts.

Step 1 — Re-pin SHAs at vendoring time (D-03 requires the ACTUAL SHA used). For each repo,
run (the SHAs in the table are HEAD at research time and may have advanced):
```
gh api repos/nelsonlaidev/nelsonlai.dev/commits/main      --jq '.sha'
gh api repos/vuesion/vuesion/commits/main                 --jq '.sha'
gh api repos/Scorpio3310/sveltekit-i18n-starter/commits/main --jq '.sha'
gh api repos/Scorpio3310/astro-i18n-starter/commits/main  --jq '.sha'
```
Record the SHA actually used for each. If `gh` is unavailable, fall back to
`git clone --depth=1 <repo> tmp && (cd tmp && git rev-parse HEAD)`.

Step 2 — Fetch and place the files. Recommended directory layout (matches RESEARCH.md):
```
tests/corpus/
  js-ts/   about.page.tsx, blog.page.tsx, home.page.tsx        (from nelsonlaidev/nelsonlai.dev)
  vue/     index.vue, services-index.vue                        (from vuesion/vuesion, PAGES ONLY)
  svelte/  Navbar.svelte, Footer.svelte, home-page.svelte       (from Scorpio3310/sveltekit-i18n-starter)
  astro/   Header.astro, Footer.astro, Info.astro               (from Scorpio3310/astro-i18n-starter)
```
Fetch each file at the pinned SHA, e.g.:
`gh api repos/{owner}/{repo}/contents/{path}?ref={sha} --jq '.content' | base64 -d > tests/corpus/{dir}/{name}`
(or `git clone --depth=1` the repo and copy the listed paths). Rename to the flat names above.

Files to vendor (PINNED — copy from RESEARCH.md):
- JS/TS (nelsonlaidev/nelsonlai.dev, MIT): src/app/[locale]/(main)/about/page.tsx,
  blog/page.tsx, (main)/page.tsx → about.page.tsx, blog.page.tsx, home.page.tsx.
  Pattern: `const t = useTranslations()` then `t("common.labels.about")` etc.
- Vue (vuesion/vuesion, MIT): src/pages/index.vue, src/pages/services/index.vue →
  index.vue, services-index.vue. Pattern: `const { t } = useI18n()` + `t('pages.index.title')`.
  VENDOR PAGES ONLY — component files use `$t(...)` template global and won't match `t`.
- Svelte (Scorpio3310/sveltekit-i18n-starter, MIT): src/components/Navbar.svelte,
  Footer.svelte, src/routes/[lang=lang]/+page.svelte → Navbar.svelte, Footer.svelte,
  home-page.svelte. Pattern: `import { t } from "$i18n/i18n"` + `t("home.h1")`.
- Astro (Scorpio3310/astro-i18n-starter, MIT): src/components/Header.astro, Footer.astro,
  Info.astro → Header.astro, Footer.astro, Info.astro. Pattern: frontmatter
  `const t = useTranslations(lang)` + `t("main.title")`.

Step 3 — VERIFY each vendored file actually contains a plain `t("...")` call. If a fetched
file (e.g. blog/page.tsx or home/page.tsx per Assumption A3 in RESEARCH) does NOT contain a
`t("key")` call, pick an alternative file from the SAME repo that does, and record the
substitution in SOURCES.md. A corpus file with zero `t()` calls passes the diff vacuously
(RESEARCH Pitfall 1) — avoid it.

Step 4 — Write `tests/corpus/SOURCES.md` (D-03 manifest). For each of the four sources record:
upstream repo URL, the EXACT commit SHA used (from Step 1), the original upstream paths taken,
the local vendored filenames, and the license (all MIT). Include a one-line note per source on
which extraction mechanism it exercises (all use `t("key")` function calls; only Vue corpus +
existing repo fixtures use the `i18nKey` attribute — Pitfall 6). Add a header line stating these
files are vendored third-party source, parsed-not-executed, and excluded from the build/test graph.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path'); const root='tests/corpus'; if(!fs.existsSync(p.join(root,'SOURCES.md')))throw new Error('SOURCES.md missing'); const dirs=['js-ts','vue','svelte','astro']; for(const d of dirs){const dir=p.join(root,d); if(!fs.existsSync(dir))throw new Error('missing corpus dir '+d); const files=fs.readdirSync(dir); if(files.length===0)throw new Error('empty corpus dir '+d);} const sm=fs.readFileSync(p.join(root,'SOURCES.md'),'utf8'); if(!/MIT/.test(sm))throw new Error('SOURCES.md missing license info'); if(!/[0-9a-f]{40}/.test(sm))throw new Error('SOURCES.md missing a 40-char commit SHA'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/corpus/js-ts/`, `vue/`, `svelte/`, `astro/` each contain ≥1 vendored file
    - Each vendored file contains a plain `t("...")` call (grep `t(["']` finds a match per framework dir)
    - `tests/corpus/SOURCES.md` exists, contains the word `License`/`MIT`, and a 40-char commit SHA per source
    - `tests/corpus/SOURCES.md` lists upstream URL + upstream paths + local filenames per source
    - No vendored corpus file is imported by any `src/**` or `scripts/**` module (parsed-not-executed)
  </acceptance_criteria>
  <done>tests/corpus/ holds one real MIT-licensed OSS project per framework, each exercising plain t("key") extraction; SOURCES.md records full provenance (URL, SHA, paths, license) for all four.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream OSS repo → vendored corpus file | Third-party source copied into the repo; only ever PARSED by the workspace compilers, never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | (n/a) | Local dev tooling — gitignore/devDep/tsconfig/corpus vendoring | accept | No applicable ASVS L1 threat: no auth, no user input, no runtime network/IO beyond the gitignored `scratch/` dir. Per RESEARCH.md "Security Domain", no ASVS category applies. |
| T-05-02 | Tampering (supply chain) | Vendored third-party corpus files under `tests/corpus/` | mitigate | Files are copied at a PINNED commit SHA recorded in SOURCES.md, are parsed-not-executed by the gate scripts, and MUST NOT be wired into the build (`tsup`) or test (`vitest`) module graph as importable code. Verified by the acceptance criterion that no `src/**`/`scripts/**` module imports them. |
</threat_model>

<verification>
- `pnpm typecheck` exits 0 (base + tsconfig.scripts.json)
- `pnpm test` still green (no source touched; only config + new untracked corpus)
- `.gitignore` ignores `scratch/`; `git status` does not list `scratch/` contents
- `tests/corpus/` populated for all four frameworks with a complete SOURCES.md
</verification>

<success_criteria>
- scratch/ gitignored; tsx devDep installed; scripts under typecheck with @/ alias intact
- One real MIT OSS project vendored per framework, each with verified plain t("key") usage
- SOURCES.md provenance manifest complete (URL + exact SHA + paths + license per source)
- Unblocks Plan 02 (shadow) and Plan 03 (bench), both of which read tests/corpus/
</success_criteria>

<output>
After completion, create `.planning/phases/05-shadow-comparison-perf-gate-default-flip/05-01-SUMMARY.md`
</output>
