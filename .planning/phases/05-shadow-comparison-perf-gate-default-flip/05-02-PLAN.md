---
phase: 05-shadow-comparison-perf-gate-default-flip
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/shadow-compare.ts
  - package.json
autonomous: true
requirements: [SHADOW-02, SHADOW-03]
must_haves:
  truths:
    - "scripts/shadow-compare.ts runs the regex path and the AST path over the same corpus+fixtures in one process"
    - "A machine-readable JSON report is written to scratch/ (gitignored) with per-totals: regexKeys, astKeys, falseNegatives, astOnlyGains, parseErrors"
    - "A concise human summary prints to stdout with a PASS/FAIL verdict"
    - "Any false-negative (key regex found that AST missed) sets process.exitCode = 1 — mechanically blocking the flip (SHADOW-03)"
    - "AST-only gains are documented in the report but NEVER affect the exit code (non-blocking, D-08)"
    - "pnpm shadow runs the harness and exits 0 on zero false-negatives"
  artifacts:
    - path: "scripts/shadow-compare.ts"
      provides: "differential regex-vs-AST harness over the corpus"
      min_lines: 50
    - path: "package.json"
      provides: "shadow script entry"
      contains: "\"shadow\""
  key_links:
    - from: "scripts/shadow-compare.ts"
      to: "detectUsedKeys"
      via: "imported from @/core/scanner, called twice (useAst:false / useAst:true)"
      pattern: "detectUsedKeys"
    - from: "scripts/shadow-compare.ts"
      to: "scratch/"
      via: "fs.writeFile of JSON report"
      pattern: "scratch"
    - from: "package.json shadow script"
      to: "scripts/shadow-compare.ts"
      via: "tsx scripts/shadow-compare.ts"
      pattern: "shadow-compare"
---

<objective>
Build the SHADOW-02 differential harness: `scripts/shadow-compare.ts` drives `detectUsedKeys`
twice over the vendored corpus + existing repo fixtures — once regex (`useAst:false`, the
v0.3.0 baseline), once AST (`useAst:true`) — diffs the `usedKeys` sets, writes a JSON report
to `scratch/`, prints a stdout summary, and exits non-zero on ANY false-negative (SHADOW-03).

Purpose: This is the parity proof that gates the flip. A false-negative is any key the regex
path found that the AST path missed; D-07 makes that a hard exit-1 block with no allowlist.
AST-only gains are recorded but never affect the verdict (D-08). `pnpm shadow` is an on-demand
script (NOT a permanent CI step — D-14) used once before the flip.
Output: scripts/shadow-compare.ts + a `shadow` package.json script; `pnpm shadow` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-shadow-comparison-perf-gate-default-flip/05-CONTEXT.md
@.planning/phases/05-shadow-comparison-perf-gate-default-flip/05-RESEARCH.md
@src/core/scanner/index.ts

<interfaces>
<!-- Verified contracts the executor uses directly — no codebase exploration needed. -->

detectUsedKeys (VERIFIED src/core/scanner/index.ts):
```typescript
export async function detectUsedKeys(
  files: string[],
  matchFunctions: string[],
  matchAttributes: string[],
  opts?: { cwd?: string; useAst?: boolean; maxConcurrency?: number }
): Promise<{
  usedKeys: Set<string>          // statically-resolved key strings; keys ending in "." already filtered
  fileContents: string[]
  parsedResults: ParsedFileResult[]
  parseErrors: FileParseError[]  // collect-and-continue errors; regex mode → empty array
}>
```
- `useAst: false` = regex path = the v0.3.0 baseline (Phase 4 guaranteed byte-identical).
- `useAst: true` = AST path.
- False-negative = a key in regex `usedKeys` that is NOT in AST `usedKeys`.
- AST-only gain = a key in AST `usedKeys` that is NOT in regex `usedKeys`.

FileParseError (VERIFIED src/core/scanner/parsers/types.ts):
```typescript
interface FileParseError { file: string; line?: number; message: string }
```

Harness config (PINNED — RESEARCH.md): matchFunctions = ["t"], matchAttributes = ["i18nKey"].
This covers all four corpus frameworks. Existing repo fixtures use the same and join automatically.

Corpus + fixture sources to scan:
- tests/corpus/**  (vendored in Plan 01: js-ts, vue, svelte, astro)
- src/__tests__/parsers/fixtures/**  (vue-setup.vue, vue-legacy.vue, component.svelte, page.astro)

Gate mechanism (ESTABLISHED codebase pattern): `process.exitCode = 1` on failure.
NEVER call `process.exit()`. Only `I18nSharpenError` is ever thrown.

Report location: scratch/ (gitignored in Plan 01). Use `fs.mkdir("scratch", { recursive: true })`
then `fs.writeFile`. The report is a derived artifact, regenerated each run — exit code is the
authoritative verdict (D-06), not the stored file.

Script execution: `tsx scripts/shadow-compare.ts` (tsx added as devDep in Plan 01).
ESM, Node ≥ 20: `performance`/`fs` available; use `import`, no `require`. Strict ESLint:
no `any` (`no-explicit-any: error`), use `import type` for type-only imports.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement scripts/shadow-compare.ts (regex-vs-AST diff + exit-1 gate)</name>
  <files>scripts/shadow-compare.ts</files>
  <read_first>
    - src/core/scanner/index.ts (detectUsedKeys signature + return shape; both engine branches; "." key filtering at lines ~75-79/103-108)
    - src/core/scanner/parsers/types.ts (FileParseError shape for the report)
    - src/__tests__/ast-shadow.test.ts (the scratch/ temp convention + how the AST path is driven end-to-end)
    - .planning/phases/05-shadow-comparison-perf-gate-default-flip/05-RESEARCH.md ("shadow-compare.ts Pattern" code block — copy structure; Pitfall 1 vacuous-pass; Pitfall 4 scratch/ commit)
    - .planning/phases/05-shadow-comparison-perf-gate-default-flip/05-CONTEXT.md (D-05 JSON+stdout, D-06 scratch/+exit, D-07 false-neg→exit1 hard block, D-08 gains non-blocking)
    - tests/corpus/SOURCES.md (confirm the corpus is present and which files exercise which mechanism)
  </read_first>
  <action>
Create `scripts/shadow-compare.ts`. Follow the RESEARCH.md "shadow-compare.ts Pattern" exactly.
Concrete requirements:

1. Imports: `import { detectUsedKeys } from "@/core/scanner"`, `import * as fs from "fs/promises"`,
   `import * as fsSync from "fs"`, `import * as path from "path"`. Use `import type` for any
   type-only imports (e.g. `FileParseError`).

2. Collect files from BOTH directories (recursive):
   - `CORPUS_DIR = path.resolve(process.cwd(), "tests/corpus")`
   - `FIXTURES_DIR = path.resolve(process.cwd(), "src/__tests__/parsers/fixtures")`
   Walk each directory recursively (use `fs.readdirSync(dir, { recursive: true })` or a small
   recursive helper) and keep only files with extensions `.ts .tsx .js .jsx .vue .svelte .astro`.
   EXCLUDE `SOURCES.md` and any non-source file.

3. Run BOTH engines over the same `files` array with the PINNED config:
   ```typescript
   const matchFunctions = ["t"]
   const matchAttributes = ["i18nKey"]
   const cwd = process.cwd()
   const [regexResult, astResult] = await Promise.all([
     detectUsedKeys(files, matchFunctions, matchAttributes, { cwd, useAst: false }),
     detectUsedKeys(files, matchFunctions, matchAttributes, { cwd, useAst: true })
   ])
   ```

4. Compute the diff:
   ```typescript
   const falseNegatives = [...regexResult.usedKeys].filter(k => !astResult.usedKeys.has(k)).sort()
   const astOnlyGains   = [...astResult.usedKeys].filter(k => !regexResult.usedKeys.has(k)).sort()
   ```

5. Build a structured JSON report (D-05) with `totals` { regexKeys, astKeys, falseNegatives,
   astOnlyGains, parseErrors } plus the full `falseNegatives`, `astOnlyGains`, and `parseErrors`
   arrays. Include `filesScanned: files.length`. Field names are at your discretion but MUST
   include these totals.

6. Write the report: `await fs.mkdir("scratch", { recursive: true })` then
   `await fs.writeFile("scratch/shadow-report.json", JSON.stringify(report, null, 2))`.

7. Print a concise stdout summary (D-05): filesScanned, regexKeys, astKeys, falseNegatives count,
   astOnlyGains count, parseErrors count, and a final PASS/FAIL line.

8. Gate (D-07 / D-06): if `falseNegatives.length > 0`, `console.error("FAIL: false negatives — AST
   missed keys regex found")` and set `process.exitCode = 1`. Otherwise print "PASS: zero false
   negatives". AST-only gains (D-08) are reported but NEVER affect the exit code. parseErrors are
   reported but DO NOT gate (STRICT-01 deferred).

9. Vacuous-pass guard (Pitfall 1): if `regexResult.usedKeys.size === 0`, print a WARNING to stderr
   ("WARNING: regex found zero keys across the corpus — diff is vacuous, check corpus exercises
   matchFunctions") but do NOT change the exit code (the corpus is fixed in Plan 01; this is a
   visibility aid, not a gate).

10. Wrap in a `main()` and invoke it; on any thrown `I18nSharpenError` (e.g. missing compiler),
    let it surface — do not swallow. Do NOT call `process.exit()`.

Must satisfy strict ESLint (no `any`) and `tsc -p tsconfig.scripts.json --noEmit`.
  </action>
  <verify>
    <automated>pnpm exec tsc -p tsconfig.scripts.json --noEmit && pnpm exec tsx scripts/shadow-compare.ts && node -e "const r=require('./scratch/shadow-report.json'); if(typeof r.totals.falseNegatives!=='number')throw new Error('report missing totals.falseNegatives'); if(typeof r.totals.astOnlyGains!=='number')throw new Error('report missing totals.astOnlyGains'); if(r.totals.regexKeys===0)throw new Error('vacuous: regex found 0 keys — corpus does not exercise extraction'); if(r.totals.falseNegatives!==0)throw new Error('FALSE NEGATIVES present: '+JSON.stringify(r.falseNegatives)); console.log('shadow OK: regexKeys='+r.totals.regexKeys+' astKeys='+r.totals.astKeys+' gains='+r.totals.astOnlyGains)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/shadow-compare.ts` exists and imports `detectUsedKeys` from `@/core/scanner`
    - `grep "useAst: false" scripts/shadow-compare.ts` and `grep "useAst: true" scripts/shadow-compare.ts` both match (both engines driven)
    - `grep "process.exitCode" scripts/shadow-compare.ts` matches; `grep "process.exit(" scripts/shadow-compare.ts` does NOT match
    - Running the script writes `scratch/shadow-report.json` with numeric `totals.falseNegatives` and `totals.astOnlyGains`
    - `scratch/shadow-report.json` has `totals.regexKeys > 0` (non-vacuous — corpus exercises extraction)
    - `scratch/shadow-report.json` has `totals.falseNegatives === 0` (parity proven; if not, this is a real bug to surface, not to suppress)
    - `pnpm exec tsc -p tsconfig.scripts.json --noEmit` exits 0 (no `any`, alias resolves)
  </acceptance_criteria>
  <done>shadow-compare.ts diffs regex vs AST over corpus+fixtures, writes a JSON report to scratch/, prints a PASS/FAIL summary, exits 1 on any false-negative and 0 otherwise; gains are documented but non-blocking; report shows zero false-negatives over the real corpus.</done>
</task>

<task type="auto">
  <name>Task 2: Add the "shadow" package.json script</name>
  <files>package.json</files>
  <read_first>
    - package.json (current scripts block; tsx is a devDep after Plan 01)
    - .planning/phases/05-shadow-comparison-perf-gate-default-flip/05-CONTEXT.md (D-14 — shadow is on-demand, NOT wired into CI)
  </read_first>
  <action>
Add a `"shadow"` entry to the package.json `scripts` block:
```json
"shadow": "tsx scripts/shadow-compare.ts"
```
Do NOT add this to `.github/workflows/ci.yml` — `pnpm shadow` is an on-demand pre-flip parity
proof, not a permanent CI gate (D-14; it is a transitional tool changed/removed in Phase 6).
Leave all existing scripts unchanged.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.scripts.shadow!=='tsx scripts/shadow-compare.ts')throw new Error('shadow script wrong or missing: '+p.scripts.shadow); console.log('OK')" && pnpm shadow</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `scripts.shadow` equals `tsx scripts/shadow-compare.ts`
    - `pnpm shadow` runs the harness and exits 0 (zero false-negatives over the real corpus)
    - `grep "shadow" .github/workflows/ci.yml` finds NO match (shadow is on-demand only, D-14)
  </acceptance_criteria>
  <done>`pnpm shadow` runs scripts/shadow-compare.ts and exits 0; shadow is intentionally absent from CI per D-14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored corpus file → shadow-compare parser | Third-party source PARSED by the workspace compilers; never executed |
| shadow-compare → scratch/ | Local gitignored write of a derived JSON report |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | (n/a) | scripts/shadow-compare.ts — local dev tooling | accept | No applicable ASVS L1 threat: no auth, no user input, no runtime network, no data persistence beyond the gitignored `scratch/` report. Per RESEARCH.md "Security Domain", no ASVS category applies. |
| T-05-04 | Tampering (supply chain) | Vendored corpus files read by the harness | mitigate | Corpus files are parsed-not-executed; the harness only reads them as input strings to `detectUsedKeys` and never imports/evaluates them as modules. |
</threat_model>

<verification>
- `pnpm exec tsc -p tsconfig.scripts.json --noEmit` exits 0
- `pnpm shadow` exits 0 and writes scratch/shadow-report.json
- scratch/shadow-report.json: regexKeys > 0, falseNegatives === 0, astOnlyGains documented
- `pnpm test` still green (no src touched)
- `git status` does not list scratch/ contents (gitignored in Plan 01)
</verification>

<success_criteria>
- scripts/shadow-compare.ts diffs regex vs AST over the real corpus + fixtures
- JSON report to scratch/ + stdout PASS/FAIL summary
- Exit 1 on any false-negative (hard block, SHADOW-03), exit 0 on parity; gains non-blocking (D-08)
- `pnpm shadow` script added, on-demand only (absent from CI per D-14)
- Zero false-negatives proven over the vendored corpus — clears one of the two flip gates
</success_criteria>

<output>
After completion, create `.planning/phases/05-shadow-comparison-perf-gate-default-flip/05-02-SUMMARY.md`
</output>
