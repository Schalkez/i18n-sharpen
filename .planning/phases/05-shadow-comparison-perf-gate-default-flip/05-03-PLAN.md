---
phase: 05-shadow-comparison-perf-gate-default-flip
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - scripts/bench.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [PERF-01]
must_haves:
  truths:
    - "scripts/bench.ts times the regex path and the AST path in one process over a deterministic <=50-file slice of the corpus"
    - "Timing is hand-rolled with performance.now(), a warmup pass (3) and N timed iterations (10), zero new dependencies"
    - "Pass/fail uses the median of the N timed runs; (AST median - regex median) > 100ms sets process.exitCode = 1"
    - "The 50-file slice is deterministic across machines (files.sort().slice(0,50); all files if <50, count documented)"
    - "pnpm bench runs the benchmark and exits 0 when the AST path is within the 100ms budget"
    - "pnpm bench is wired into .github/workflows/ci.yml build-test job so a perf regression fails the build"
  artifacts:
    - path: "scripts/bench.ts"
      provides: "hand-rolled live-delta regex-vs-AST benchmark with 100ms perf gate"
      min_lines: 50
    - path: "package.json"
      provides: "bench script entry"
      contains: "\"bench\""
    - path: ".github/workflows/ci.yml"
      provides: "pnpm bench CI step in the build-test job"
      contains: "pnpm bench"
  key_links:
    - from: "scripts/bench.ts"
      to: "detectUsedKeys"
      via: "imported from @/core/scanner, timed twice (useAst:false / useAst:true)"
      pattern: "detectUsedKeys"
    - from: ".github/workflows/ci.yml"
      to: "package.json bench script"
      via: "run: pnpm bench step"
      pattern: "pnpm bench"
---

<objective>
Build the PERF-01 perf gate: `scripts/bench.ts` times the regex path and the AST path
in-process over a deterministic 50-file slice of the vendored corpus (live in-process delta,
D-09 — the regex path IS the v0.3.0 baseline). Hand-rolled `performance.now()` timing with
warmup=3 and N=10, median of the timed runs (D-10/D-12). Exit non-zero when
(AST median - regex median) > 100ms. Add a `bench` package.json script and wire `pnpm bench`
into CI so a perf regression fails the build (D-14).

Purpose: This is the second of the two flip gates. The benchmark is self-calibrating (both
engines run in the same process, so it is immune to machine-to-machine variance and never
goes stale). Zero new runtime deps — the median is a 4-line formula. `pnpm shadow` stays
on-demand; only `pnpm bench` becomes a permanent CI step.
Output: scripts/bench.ts + a `bench` script + a `pnpm bench` CI step; `pnpm bench` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-shadow-comparison-perf-gate-default-flip/05-CONTEXT.md
@.planning/phases/05-shadow-comparison-perf-gate-default-flip/05-RESEARCH.md
@src/core/scanner/index.ts
@.github/workflows/ci.yml

<interfaces>
<!-- Verified contracts the executor uses directly — no codebase exploration needed. -->

detectUsedKeys (VERIFIED src/core/scanner/index.ts) — same signature the shadow harness uses:
```typescript
detectUsedKeys(files, matchFunctions, matchAttributes, { cwd, useAst }):
  Promise<{ usedKeys, fileContents, parsedResults, parseErrors }>
```
- `useAst: false` = regex path = v0.3.0 baseline (Phase 4 guaranteed byte-identical).
- `useAst: true`  = AST path. AST path uses runBoundedPool (max 4 concurrent) internally —
  do NOT re-parallelize externally; call detectUsedKeys ONCE per timed iteration over the slice.

Benchmark constants (PINNED — RESEARCH.md "Benchmark Pattern" + "Warmup and N Recommendation"):
- WARMUP = 3   (V8 JIT warm; excludes first-run GC spikes — Pitfall 3)
- N = 10       (median of 10 = P50, robust to 1-2 GC outliers; avoid N=5 too few, N=50 too slow)
- THRESHOLD = 100  (ms; (AST median - regex median) > 100 -> fail)
- Timer: `performance.now()` (Node >= 20 global, no import; NOT Date.now() — ms resolution)

Median (PINNED 4-line formula — zero deps, D-10):
```typescript
function median(arr: number[]): number {
  const sorted = [...arr].sort((a, b) => a - b)
  const mid = Math.floor(sorted.length / 2)
  return sorted.length % 2 !== 0 ? sorted[mid] : (sorted[mid - 1] + sorted[mid]) / 2
}
```

Deterministic slice (PINNED — D-11, Pitfall 2): `files.sort().slice(0, 50)`. The sort is stable
across platforms (fs.readdirSync order is OS-dependent — must sort first). If the corpus has
fewer than 50 source files (likely ~11 corpus + 4 fixtures ~= 15), use ALL of them and print the
actual count. The 100ms threshold is independent of file count.

Files to time (same as shadow harness):
- tests/corpus/**  (vendored in Plan 01)
- src/__tests__/parsers/fixtures/**

Config (PINNED): matchFunctions = ["t"], matchAttributes = ["i18nKey"].

Gate mechanism (ESTABLISHED): `process.exitCode = 1` on failure. NEVER `process.exit()`.

Current CI (VERIFIED .github/workflows/ci.yml) — build-test job steps in order:
checkout -> install pnpm -> setup node 20 -> pnpm install --frozen-lockfile -> pnpm lint ->
pnpm typecheck -> pnpm test -> pnpm build. D-14 adds a `pnpm bench` step. Place it AFTER
`pnpm test` and BEFORE `pnpm build` so a perf regression blocks the build artifact.

Script execution: `tsx scripts/bench.ts`. ESM, Node >= 20, strict ESLint (no `any`, `import type`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement scripts/bench.ts (live regex-vs-AST delta + 100ms perf gate)</name>
  <files>scripts/bench.ts</files>
  <read_first>
    - src/core/scanner/index.ts (detectUsedKeys; the AST path already uses runBoundedPool max 4 — do not double-parallelize)
    - src/core/scanner/pool.ts (runBoundedPool — confirms the concurrent path the bench measures)
    - .planning/phases/05-shadow-comparison-perf-gate-default-flip/05-RESEARCH.md ("Benchmark Pattern" code block; "Warmup and N Recommendation"; "Deterministic 50-File Slice Strategy"; Pitfall 2 determinism, Pitfall 3 GC)
    - .planning/phases/05-shadow-comparison-perf-gate-default-flip/05-CONTEXT.md (D-09 live delta, D-10 hand-rolled zero-dep, D-11 50-file slice, D-12 median + exit>100ms)
    - scripts/shadow-compare.ts (Plan 02 — reuse the same recursive file-collection + extension filter helper logic for consistency)
  </read_first>
  <action>
Create `scripts/bench.ts`. Follow the RESEARCH.md "Benchmark Pattern" exactly. Concrete requirements:

1. Imports: `import { detectUsedKeys } from "@/core/scanner"`, `import * as fs from "fs"`,
   `import * as path from "path"`. Use `import type` where applicable. No new dependency — the
   median is the 4-line formula below; `performance.now()` is a Node >= 20 global (no import).

2. Constants at top of file:
   ```typescript
   const WARMUP = 3
   const N = 10
   const THRESHOLD_MS = 100
   ```

3. Collect source files from `tests/corpus/` and `src/__tests__/parsers/fixtures/` (same
   extension filter as the shadow harness: `.ts .tsx .js .jsx .vue .svelte .astro`; exclude
   SOURCES.md / non-source). Then build the deterministic slice (D-11, Pitfall 2):
   ```typescript
   const slice = allFiles.sort().slice(0, 50)
   ```
   Print the actual slice count (`Benchmarking ${slice.length} files`). If <50, that is expected
   and fine — the threshold is count-independent. Do NOT fetch additional files; do NOT pad by
   default (the research notes padding is optional and only if a 50-floor is later required —
   for this phase, all-corpus <=50 is acceptable per Open Question #1).

4. The timing helper (PINNED structure from RESEARCH):
   ```typescript
   async function timeEngine(files: string[], useAst: boolean): Promise<number[]> {
     const matchFunctions = ["t"]
     const matchAttributes = ["i18nKey"]
     const cwd = process.cwd()
     for (let i = 0; i < WARMUP; i++) {
       await detectUsedKeys(files, matchFunctions, matchAttributes, { cwd, useAst })
     }
     const durations: number[] = []
     for (let i = 0; i < N; i++) {
       const start = performance.now()
       await detectUsedKeys(files, matchFunctions, matchAttributes, { cwd, useAst })
       durations.push(performance.now() - start)
     }
     return durations
   }
   ```
   Call `detectUsedKeys` ONCE per iteration over the whole slice — do not wrap it in an extra
   Promise.all over files (the AST path's runBoundedPool already bounds concurrency to 4).

5. The median helper (PINNED 4-line formula, zero deps — copy verbatim from the interfaces block).

6. main(): time regex first (`timeEngine(slice, false)`), then AST (`timeEngine(slice, true)`).
   Compute `regexMedian`, `astMedian`, `delta = astMedian - regexMedian`. Print:
   ```
   Regex median: {regexMedian.toFixed(1)}ms
   AST median:   {astMedian.toFixed(1)}ms
   Delta:        {delta.toFixed(1)}ms (budget: <=100ms, files: {slice.length})
   ```

7. Gate (D-12): if `delta > THRESHOLD_MS`, `console.error("FAIL: AST is {delta}ms slower than
   regex — exceeds 100ms budget")` and set `process.exitCode = 1`. Otherwise print
   "PASS: AST within perf budget". NEVER call `process.exit()`.

Must satisfy strict ESLint (no `any`) and `tsc -p tsconfig.scripts.json --noEmit`.
  </action>
  <verify>
    <automated>pnpm exec tsc -p tsconfig.scripts.json --noEmit && pnpm exec tsx scripts/bench.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/bench.ts` exists and imports `detectUsedKeys` from `@/core/scanner`
    - `grep "performance.now" scripts/bench.ts` matches; `grep "Date.now" scripts/bench.ts` does NOT match
    - `grep "useAst: false" scripts/bench.ts` and `grep "useAst: true" scripts/bench.ts` both match (both engines timed)
    - `.sort()` appears before `.slice(` in scripts/bench.ts (deterministic slice)
    - `grep "process.exitCode" scripts/bench.ts` matches; `grep "process.exit(" scripts/bench.ts` does NOT match
    - WARMUP=3, N=10, THRESHOLD 100 present as constants
    - `pnpm exec tsx scripts/bench.ts` exits 0 (AST within the 100ms budget over the real corpus)
    - `pnpm exec tsc -p tsconfig.scripts.json --noEmit` exits 0 (no `any`)
  </acceptance_criteria>
  <done>bench.ts times regex vs AST in-process over a deterministic <=50-file slice using performance.now() + warmup + median, exits 1 only when AST-regex > 100ms; exits 0 over the real corpus.</done>
</task>

<task type="auto">
  <name>Task 2: Add the "bench" package.json script and wire pnpm bench into CI</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <read_first>
    - package.json (current scripts; `shadow` added in Plan 02; tsx is a devDep from Plan 01)
    - .github/workflows/ci.yml (current build-test job step order: lint -> typecheck -> test -> build)
    - .planning/phases/05-shadow-comparison-perf-gate-default-flip/05-CONTEXT.md (D-14 — bench IS a permanent CI gate; shadow is NOT)
    - .planning/phases/05-shadow-comparison-perf-gate-default-flip/05-RESEARCH.md ("CI Workflow Current State" — place bench after test, before build)
  </read_first>
  <action>
1. Add a `"bench"` entry to package.json `scripts`:
   ```json
   "bench": "tsx scripts/bench.ts"
   ```
   Leave the `shadow` script (Plan 02) and all other scripts unchanged.

2. Edit `.github/workflows/ci.yml`: add a new step to the `build-test` job, placed AFTER the
   "Run Tests" (`pnpm test`) step and BEFORE the "Build" (`pnpm build`) step, so a perf
   regression fails the build (D-14, PERF-01 "fails the build"):
   ```yaml
       - name: Benchmark (perf gate)
         run: pnpm bench
   ```
   Do NOT add a `pnpm shadow` step — shadow stays on-demand only (D-14). Keep all existing
   steps and their order intact.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.scripts.bench!=='tsx scripts/bench.ts')throw new Error('bench script wrong/missing: '+p.scripts.bench); const ci=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); if(!/pnpm bench/.test(ci))throw new Error('pnpm bench not in ci.yml'); if(/pnpm shadow/.test(ci))throw new Error('pnpm shadow must NOT be in ci.yml (D-14)'); const tIdx=ci.indexOf('pnpm test'),bIdx=ci.indexOf('pnpm bench'),buildIdx=ci.indexOf('pnpm build'); if(!(tIdx<bIdx&&bIdx<buildIdx))throw new Error('bench step must be after test and before build'); console.log('OK')" && pnpm bench</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `scripts.bench` equals `tsx scripts/bench.ts`
    - `.github/workflows/ci.yml` contains a `run: pnpm bench` step in the build-test job
    - The `pnpm bench` step sits AFTER `pnpm test` and BEFORE `pnpm build` in ci.yml
    - `.github/workflows/ci.yml` does NOT contain `pnpm shadow` (D-14 — shadow on-demand only)
    - `pnpm bench` exits 0 over the real corpus
  </acceptance_criteria>
  <done>`pnpm bench` script added and wired into CI (after test, before build) so perf regression fails the build; shadow intentionally absent from CI per D-14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored corpus file -> bench parser | Third-party source PARSED by the workspace compilers; never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | (n/a) | scripts/bench.ts + CI step — local dev tooling | accept | No applicable ASVS L1 threat: no auth, no user input, no runtime network, no data persistence. The bench reads vendored files and prints timings; CI runs it as a gate. Per RESEARCH.md "Security Domain", no ASVS category applies. |
| T-05-06 | Tampering (supply chain) | Vendored corpus files read by the benchmark | mitigate | Corpus files are parsed-not-executed; the bench only reads them as input strings to `detectUsedKeys` and never imports/evaluates them as modules. |
</threat_model>

<verification>
- `pnpm exec tsc -p tsconfig.scripts.json --noEmit` exits 0
- `pnpm bench` exits 0 (AST within the 100ms budget over the real corpus)
- `.github/workflows/ci.yml` runs `pnpm bench` after test, before build; no `pnpm shadow` step
- `pnpm test` still green (no src touched)
</verification>

<success_criteria>
- scripts/bench.ts: hand-rolled live regex-vs-AST delta over a deterministic <=50-file slice
- performance.now() + warmup=3 + N=10 + median; exits 1 only when delta > 100ms
- `bench` script added and wired into CI (after test, before build) so perf regression fails the build
- shadow stays on-demand (absent from CI); the second of the two flip gates is in place and green
</success_criteria>

<output>
After completion, create `.planning/phases/05-shadow-comparison-perf-gate-default-flip/05-03-SUMMARY.md`
</output>
