---
phase: 03-framework-parsers-dispatcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/__tests__/parsers/fixtures/vue-setup.vue
  - src/__tests__/parsers/fixtures/vue-legacy.vue
  - src/__tests__/parsers/fixtures/component.svelte
  - src/__tests__/parsers/fixtures/page.astro
  - src/__tests__/parsers/vue.test.ts
  - src/__tests__/parsers/svelte.test.ts
  - src/__tests__/parsers/astro.test.ts
  - src/__tests__/parsers/dispatcher.test.ts
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "@vue/compiler-sfc, svelte, @astrojs/compiler are declared optional peer deps and installed as devDependencies so framework parsers and their tests can run"
    - "RED-state test files exist for vue/svelte/astro/dispatcher that import the not-yet-written parsers"
    - "Fixture files with KNOWN line numbers exist for every framework"
  artifacts:
    - path: "package.json"
      provides: "framework compilers as optionalDependencies/peerDependenciesMeta + devDependencies"
      contains: "@vue/compiler-sfc"
    - path: "src/__tests__/parsers/fixtures/vue-setup.vue"
      provides: "Vue <script setup> fixture with key on a known line"
    - path: "src/__tests__/parsers/fixtures/vue-legacy.vue"
      provides: "Vue Options-API <script> fixture (parity target)"
    - path: "src/__tests__/parsers/fixtures/component.svelte"
      provides: "Svelte fixture with instance + module script and template attr/text"
    - path: "src/__tests__/parsers/fixtures/page.astro"
      provides: "Astro fixture with frontmatter + template text/attr"
    - path: "src/__tests__/parsers/vue.test.ts"
      provides: "RED test scaffold for parseVueFile"
      contains: "parseVueFile"
    - path: "src/__tests__/parsers/svelte.test.ts"
      provides: "RED test scaffold for parseSvelteFile"
      contains: "parseSvelteFile"
    - path: "src/__tests__/parsers/astro.test.ts"
      provides: "RED test scaffold for parseAstroFile"
      contains: "parseAstroFile"
    - path: "src/__tests__/parsers/dispatcher.test.ts"
      provides: "RED test scaffold for parseFile dispatcher"
      contains: "parseFile"
  key_links:
    - from: "src/__tests__/parsers/vue.test.ts"
      to: "src/core/scanner/parsers/vue.ts"
      via: "import { parseVueFile }"
      pattern: "from .*parsers/vue"
    - from: "package.json"
      to: "node_modules/@vue/compiler-sfc"
      via: "pnpm add -D"
      pattern: "@vue/compiler-sfc"
---

<objective>
Establish the blocking prerequisites for every Phase 3 parser: declare the three
framework compilers as optional peer deps, install them as devDependencies so the
test suite can actually parse `.vue/.svelte/.astro` source, and lay down the RED-state
test files + fixtures that the parser plans (02-04) and the dispatcher plan (05) will
turn green.

Purpose: Research found `package.json` is MISSING `@vue/compiler-sfc`, `svelte`, and
`@astrojs/compiler` (verified: all three fail `require.resolve`). Without these, NO
framework parser or test can run — this is a hard blocker for the entire phase. The
Nyquist validation contract also requires the test scaffolds + fixtures to exist before
any production parser code is written.

Output: Updated `package.json` (peer + dev deps), 4 fixture files with documented line
numbers, and 4 test files that fail at import time (RED) because the parsers do not yet
exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-framework-parsers-dispatcher/03-CONTEXT.md
@.planning/phases/03-framework-parsers-dispatcher/03-RESEARCH.md
@.planning/phases/03-framework-parsers-dispatcher/03-VALIDATION.md

<interfaces>
<!-- LOCKED contracts the test scaffolds assert against — do NOT change these files. -->

From src/core/scanner/parsers/types.ts (LOCKED):
```typescript
export interface ParsedFileResult {
  usedKeys: { key: string; offset: number }[]
  dynamicCalls: {
    expression: string
    arg: string
    offset: number
    classification: "fully-dynamic" | "structured-concat"
    prefix?: string
  }[]
  hardcodedCandidates: { text: string; offset: number }[]
}
export interface FileParseError { file: string; line?: number; message: string }
```

From src/core/scanner/lines.ts (LOCKED, reused for line assertions — OFFSET-02):
```typescript
export function computeLineOffsets(content: string): number[]
export function offsetToLine(offsets: number[], offset: number): number
```

From src/core/errors.ts (LOCKED):
```typescript
// I18nSharpenError; err.error is a discriminated union.
// Missing-compiler variant: { kind: "missing-dependency", packageName, installCommand, message }
```

The dispatcher signature every test scaffold targets (D-02, async):
```typescript
parseFile(source, filePath, matchFunctions, matchAttributes, cwd):
  Promise<{ result: ParsedFileResult; errors: FileParseError[] }>
// Per-parser parseVueFile/parseSvelteFile/parseAstroFile share this exact shape.
```

Existing test idiom (src/__tests__/parsers/typescript.test.ts): vitest `describe/it/expect`,
inline source strings, `process.cwd()` passed as `cwd`, default `matchFunctions = ["t","i18n.t"]`,
`matchAttributes = ["i18nKey"]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare framework compilers as optional peer deps + install as devDependencies</name>
  <read_first>
    - package.json (the file being modified — note current `peerDependencies`/`peerDependenciesMeta` only have `typescript`)
    - .planning/phases/03-framework-parsers-dispatcher/03-RESEARCH.md §"package.json Status" (exact JSON block + verified versions)
  </read_first>
  <action>
    Edit `package.json` to add the three framework compilers as OPTIONAL peer deps, mirroring
    the existing `typescript` entry. After editing, the `peerDependencies` and
    `peerDependenciesMeta` blocks MUST read EXACTLY:

    ```json
    "peerDependencies": {
      "@astrojs/compiler": ">=4.0.0",
      "@vue/compiler-sfc": ">=3.0.0",
      "svelte": ">=4.0.0",
      "typescript": ">=5.0"
    },
    "peerDependenciesMeta": {
      "@astrojs/compiler": { "optional": true },
      "@vue/compiler-sfc": { "optional": true },
      "svelte": { "optional": true },
      "typescript": { "optional": true }
    },
    ```

    Then install them as devDependencies so the test suite can actually parse fixtures
    (research verified all three fail `require.resolve` today). Run EXACTLY:

    ```
    pnpm add -D @vue/compiler-sfc@^3.5.35 svelte@^5.56.0 @astrojs/compiler@^4.0.0
    ```

    These pinned versions are VERIFIED current as of 2026-06-01 (npm registry).
    Do NOT add them to `dependencies` — they must stay out of the bundled runtime tree
    (DEP-01: the CLI ships ~0 framework weight). `pnpm add -D` will also write the matching
    devDependencies entries; leave the peer block as authored above (do not let the installer
    overwrite the `>=` ranges with exact pins).
  </action>
  <verify>
    <automated>node -e "require.resolve('@vue/compiler-sfc');require.resolve('svelte/compiler');require.resolve('@astrojs/compiler')" && node -e "const p=require('./package.json');const m=p.peerDependenciesMeta;process.exit(m['@vue/compiler-sfc'].optional&&m['svelte'].optional&&m['@astrojs/compiler'].optional?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require.resolve('@vue/compiler-sfc')"` exits 0 (installed)
    - `node -e "require.resolve('svelte/compiler')"` exits 0
    - `node -e "require.resolve('@astrojs/compiler')"` exits 0
    - `package.json` `peerDependenciesMeta` marks all three `{ "optional": true }`
    - `package.json` `dependencies` does NOT contain `@vue/compiler-sfc`, `svelte`, or `@astrojs/compiler` (they live only under `devDependencies` + `peerDependencies`)
    - `pnpm build` still succeeds (no new bundled runtime dep)
  </acceptance_criteria>
  <done>All three compilers resolve via Node; package.json declares them optional peer deps + devDeps; build is unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Create framework fixtures with documented known line numbers</name>
  <read_first>
    - src/core/scanner/parsers/typescript.ts (the delegated parser — fixtures must contain `t("...")` calls and `i18nKey="..."` attributes it can extract)
    - src/core/scanner/hardcoded.ts (SKIP_TAGS / HARDCODED_ATTRS — the template-text parity target)
    - .planning/phases/03-framework-parsers-dispatcher/03-RESEARCH.md §"Critical Test Seams for TEST-04" (fixture shapes for parity + line-number assertions)
  </read_first>
  <action>
    Create four fixture files under `src/__tests__/parsers/fixtures/`. Each must contain a
    `t("...")` call inside its embedded script block on a KNOWN line, plus a template
    `i18nKey="..."` attribute and a hardcoded template text node, so plans 02-05 can assert
    exact line numbers (FW-04). Add a top-of-file comment in each fixture documenting the
    1-based line each key sits on. Use these EXACT contents (line numbers are load-bearing):

    `vue-setup.vue` — key `hero.title` on line 3, template attr `nav.home`, text "Welcome":
    ```vue
    <script setup lang="ts">
    // line-comment to push the call down
    const x = t('hero.title')
    </script>
    <template>
      <div i18nKey="nav.home">Welcome</div>
    </template>
    ```

    `vue-legacy.vue` — semantically equivalent Options-API form, key `hero.title`, attr `nav.home`:
    ```vue
    <script lang="ts">
    export default {
      setup() {
        return { x: t('hero.title') }
      }
    }
    </script>
    <template>
      <div i18nKey="nav.home">Welcome</div>
    </template>
    ```

    `component.svelte` — module + instance scripts + template attr/text:
    ```svelte
    <script context="module" lang="ts">
      const moduleKey = t('mod.init')
    </script>
    <script lang="ts">
      const x = t('page.title')
    </script>
    <div i18nKey="nav.home">Hello Svelte</div>
    ```

    `page.astro` — frontmatter key `page.title` on line 2, template attr/text:
    ```astro
    ---
    const x = t('page.title')
    ---
    <h1 i18nKey="nav.home">Hello Astro</h1>
    ```

    Do NOT add these fixtures to any tsconfig `include` glob — they are raw text loaded via
    `fs.readFileSync` in tests, never compiled by the project.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['vue-setup.vue','vue-legacy.vue','component.svelte','page.astro'].forEach(f=>{const s=fs.readFileSync('src/__tests__/parsers/fixtures/'+f,'utf8');if(!s.includes('i18nKey')||!s.includes('t('))throw new Error('bad fixture '+f)});console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/parsers/fixtures/vue-setup.vue` exists and `t('hero.title')` is on line 3 (verify: `node -e "console.log(require('fs').readFileSync('src/__tests__/parsers/fixtures/vue-setup.vue','utf8').split('\n')[2].includes(\"t('hero.title')\"))"` prints `true`)
    - `src/__tests__/parsers/fixtures/page.astro` has `t('page.title')` on line 2
    - Every fixture contains both an `i18nKey="..."` attribute and a hardcoded text node
    - `vue-setup.vue` and `vue-legacy.vue` both contain `hero.title` and `nav.home` (parity pair)
  </acceptance_criteria>
  <done>Four fixtures exist with documented line numbers; each carries an embedded-script key, a template attr key, and a template text node.</done>
</task>

<task type="auto">
  <name>Task 3: Write RED-state test scaffolds for the three parsers + dispatcher</name>
  <read_first>
    - src/__tests__/parsers/typescript.test.ts (the existing test idiom to mirror: vitest, inline `parse()` helper, `process.cwd()`)
    - src/__tests__/parsers/resolve.test.ts (the missing-dependency assertion pattern: `e.error.kind === "missing-dependency"`, `installCommand` matches `/add -[Dd]|install -D/`)
    - src/core/scanner/lines.ts (computeLineOffsets/offsetToLine for the line-number seam)
  </read_first>
  <action>
    Create four test files that import the not-yet-existing parsers (so they fail RED at import
    time) and encode the six critical seams from 03-VALIDATION.md. Each test loads its fixture
    via `fs.readFileSync(path.join(__dirname, "fixtures", "<name>"), "utf8")` and calls the parser
    with `(source, "<fixturePath>", ["t","i18n.t"], ["i18nKey"], process.cwd())`.

    `src/__tests__/parsers/vue.test.ts` — `import { parseVueFile } from "@/core/scanner/parsers/vue"`:
      - FW-01 parity: `vue-setup.vue` and `vue-legacy.vue` both yield `usedKeys` containing `hero.title` AND `nav.home`.
      - FW-04 line number: for `vue-setup.vue`, the `hero.title` key's offset maps via
        `offsetToLine(computeLineOffsets(src), offset)` to line **3**.
      - FW-04 attr: template `i18nKey="nav.home"` is in `usedKeys`; "Welcome" is in `hardcodedCandidates`.
      - D-17 collect-and-continue: a broken `<script setup>` (`const = ;`) yields `errors.length >= 1`,
        `errors[0].file` set, `result.usedKeys` empty, nothing thrown.
      - FW-05 missing compiler: `vi.spyOn(resolveModule,"loadWorkspaceDep").mockImplementation(()=>{throw new I18nSharpenError({kind:"missing-dependency",packageName:"@vue/compiler-sfc",installCommand:"pnpm add -D @vue/compiler-sfc",message:"..."})})` → `await expect(parseVueFile(...)).rejects.toThrow(I18nSharpenError)` and the rejected error has `kind: "missing-dependency"`.

    `src/__tests__/parsers/svelte.test.ts` — `import { parseSvelteFile } from "@/core/scanner/parsers/svelte"`:
      - FW-02 v5: `component.svelte` parse yields `usedKeys` containing `page.title`, `mod.init`, `nav.home`; no crash.
      - FW-02 v4 mode: drive the legacy path (mock the version gate to report `4.2.0`) and assert the
        same keys; no crash, non-empty result. (The exact mock mechanism is finalized in plan 03;
        the scaffold may mark this `it` with a TODO and a failing assertion so it stays RED.)
      - FW-04 line number: `page.title` key offset maps to its known line in `component.svelte`.
      - D-17 + FW-05: same shapes as vue (broken instance script → collected error; mocked
        missing `svelte/compiler` → rejects with `missing-dependency`).

    `src/__tests__/parsers/astro.test.ts` — `import { parseAstroFile } from "@/core/scanner/parsers/astro"`:
      - FW-03 concurrency: `await Promise.all(Array.from({length:10},()=>parseAstroFile(src,"page.astro",["t"],["i18nKey"],cwd)))`
        → all 10 `result.usedKeys` identical (each contains `page.title`).
      - FW-04 line number: `page.title` frontmatter key offset maps to line **2** of `page.astro`.
      - D-17 + FW-05: broken frontmatter → collected error; mocked missing `@astrojs/compiler` → rejects.

    `src/__tests__/parsers/dispatcher.test.ts` — `import { parseFile } from "@/core/scanner/parsers"`:
      - PARSE-06 routing: `.ts` source through `parseFile` returns the same `usedKeys` as
        `parseTypeScriptFile` directly (e.g. `t("a.b")` → `usedKeys` has `a.b`).
      - PARSE-06 unknown ext: `parseFile("x", "x.txt", ["t"], [], cwd)` resolves to
        `{ result: { usedKeys: [], dynamicCalls: [], hardcodedCandidates: [] }, errors: [] }` — no throw.
      - `.vue/.svelte/.astro` route to their respective parsers (assert a key extracted from each fixture).

    Use `vi.spyOn`/`vi.mock` on `@/core/scanner/parsers/resolve` for the missing-compiler cases —
    NOTE the module-level `depCache` in resolve.ts: spy BEFORE the first real load, or the cached
    module short-circuits the spy. Prefer `vi.spyOn(resolveModule, "loadWorkspaceDep")` with
    `mockImplementationOnce`/`mockRestore` in a `try/finally` so other tests still see the real loader.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const need={'vue.test.ts':'parseVueFile','svelte.test.ts':'parseSvelteFile','astro.test.ts':'parseAstroFile','dispatcher.test.ts':'parseFile'};for(const[f,sym]of Object.entries(need)){const s=fs.readFileSync('src/__tests__/parsers/'+f,'utf8');if(!s.includes(sym))throw new Error(f+' missing '+sym)}console.log('scaffolds ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/parsers/vue.test.ts` contains `parseVueFile` and an `offsetToLine(` assertion expecting `3`
    - `src/__tests__/parsers/svelte.test.ts` contains `parseSvelteFile`
    - `src/__tests__/parsers/astro.test.ts` contains `parseAstroFile` and `Promise.all(` with length `10`
    - `src/__tests__/parsers/dispatcher.test.ts` contains `parseFile` and an unknown-extension empty-result assertion
    - `pnpm vitest run src/__tests__/parsers/vue.test.ts` FAILS at import resolution (RED — parser file does not exist yet); this is the expected Nyquist RED state, NOT a plan failure
    - `pnpm vitest run src/__tests__/parsers/typescript.test.ts` and `resolve.test.ts` still PASS (scaffolds did not break existing tests)
  </acceptance_criteria>
  <done>Four test files exist importing the future parsers/dispatcher, encoding all six critical seams; they fail RED on missing modules while pre-existing parser tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace deps → tool | `pnpm add -D` pulls third-party compiler packages into the dev tree |
| fixture files → test parser | Untrusted-shaped source strings fed to (future) framework `parse()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | devDependency install of `@vue/compiler-sfc`/`svelte`/`@astrojs/compiler` | accept | Pinned to verified versions (`^3.5.35`/`^5.56.0`/`^4.0.0`); installed dev-only, never bundled into the published runtime tree (DEP-01). If the workspace is compromised the tool is not the security boundary (per RESEARCH §Security Domain). |
| T-03-02 | Denial of Service | malformed fixture parsed by tests | mitigate | Fixtures are small, fixed, repo-controlled strings; no pathological/large inputs in this plan. Collect-and-continue (D-17) is asserted by the RED scaffolds so a bad block never aborts a run. |
| T-03-03 | Elevation of Privilege | parser must never execute parsed source | mitigate | Scaffolds assert parsers only READ/extract (no `eval`/execution); fixtures contain inert `t("...")` strings, never executed. |
</threat_model>

<verification>
- `node -e "require.resolve('@vue/compiler-sfc')"` / `svelte/compiler` / `@astrojs/compiler` all exit 0
- `package.json` declares all three as optional peer deps + devDeps; none in `dependencies`
- `pnpm build` succeeds (no new runtime dep bundled)
- Four fixtures exist with documented line numbers; four test scaffolds exist importing the future parsers/dispatcher
- Pre-existing `typescript.test.ts` + `resolve.test.ts` remain green; new scaffolds are RED on missing modules (expected)
</verification>

<success_criteria>
- All three framework compilers resolvable + declared optional peer deps (unblocks all of Phase 3)
- RED test scaffolds + fixtures in place for vue/svelte/astro/dispatcher encoding the six critical TEST-04 seams
- Existing test suite unaffected; build still green
</success_criteria>

<output>
After completion, create `.planning/phases/03-framework-parsers-dispatcher/03-01-SUMMARY.md`
</output>
