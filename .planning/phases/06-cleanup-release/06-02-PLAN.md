---
phase: 06-cleanup-release
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - src/core/scanner/text.ts
  - src/core/scanner/hardcoded.ts
  - src/commands/validate.ts
  - src/__tests__/hardcoded.test.ts
autonomous: true
requirements: [CLEAN-01]
must_haves:
  truths:
    - "isHardcodedIgnored lives in text.ts and is importable from @/core/scanner/text"
    - "validate.ts's AST-branch call to isHardcodedIgnored resolves from its new home (typecheck passes)"
    - "hardcoded.test.ts's isHardcodedIgnored tests pass against the function's new home"
    - "hardcoded.ts no longer defines isHardcodedIgnored (it is moved, not copied-and-orphaned)"
  artifacts:
    - path: "src/core/scanner/text.ts"
      provides: "isHardcodedIgnored (moved from hardcoded.ts)"
      contains: "isHardcodedIgnored"
  key_links:
    - from: "src/commands/validate.ts"
      to: "src/core/scanner/text.ts (isHardcodedIgnored)"
      via: "import resolved through @/core/scanner barrel or @/core/scanner/text"
      pattern: "isHardcodedIgnored"
    - from: "src/__tests__/hardcoded.test.ts"
      to: "src/core/scanner/text.ts (isHardcodedIgnored)"
      via: "import from @/core/scanner/text"
      pattern: "@/core/scanner/text"
---

<objective>
Move `isHardcodedIgnored` from `src/core/scanner/hardcoded.ts` into `src/core/scanner/text.ts`
and repoint every importer, as the first isolated step of the deletion spine. This is a hard
pre-condition (STATE.md carry-forward, RESEARCH Pitfall 1): `isHardcodedIgnored` is a SURVIVING
function called inside the AST branch of `validate.ts` (line ~314), so it must reach its new home
before `hardcoded.ts` can be deleted in Plan 03 — otherwise the typecheck breaks.

Purpose: Make `text.ts` the single home for the surviving text-detection helper so `hardcoded.ts`
becomes safely deletable. No behavior changes — same function, new module.

Output: `isHardcodedIgnored` defined in `text.ts`, removed from `hardcoded.ts`; `validate.ts`
import resolving correctly; `hardcoded.test.ts` isHardcodedIgnored tests importing from the new
home and passing. The quality gate stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanup-release/06-RESEARCH.md

# Source of truth for the move + import sites:
@src/core/scanner/hardcoded.ts
@src/core/scanner/text.ts
@src/commands/validate.ts
@src/core/scanner/index.ts
@src/__tests__/hardcoded.test.ts

<interfaces>
<!-- isHardcodedIgnored signature (confirm exact signature in hardcoded.ts before moving). -->
<!-- It is called at validate.ts ~line 314 inside the AST branch — a SURVIVING caller. -->

isHardcodedIgnored(text: string, ignoreHardcoded?: string[] | undefined): boolean
  // (confirm the exact parameter list and default-ignore behavior from hardcoded.ts)

Current barrel (src/core/scanner/index.ts) re-exports:
  export * from "./files"
  export * from "./regex"      // deleted in Plan 03
  export * from "./text"       // <- isHardcodedIgnored will flow through here after the move
  export * from "./dynamic"    // deleted in Plan 03
  export * from "./lines"
  export * from "./hardcoded"  // deleted in Plan 03

Because index.ts already does `export * from "./text"`, once isHardcodedIgnored moves into
text.ts it is automatically re-exported by the @/core/scanner barrel. validate.ts may keep
importing it from "@/core/scanner" OR switch to "@/core/scanner/text" — either resolves.
</interfaces>
</context>

<project_compliance>
This repo is GitNexus-indexed (see ./CLAUDE.md "Always Do"). For this plan:
- **Impact analysis is already satisfied** by `06-RESEARCH.md`, which enumerated the full blast
  radius of `isHardcodedIgnored` (its surviving caller at `validate.ts` ~line 314, the barrel
  re-export, and the test importer) from direct reads. Treat RESEARCH as the `gitnexus_impact`
  result — no separate impact run required before the move.
- **Before each commit**, run `gitnexus_detect_changes()` to confirm only the expected symbols/files
  changed (CLAUDE.md MUST). If a GitNexus tool reports the index is stale, run `npx gitnexus analyze`
  first; if GitNexus is unavailable, note that in the SUMMARY and proceed (the `pnpm tsc --noEmit`
  gate is the hard correctness backstop).
</project_compliance>

<tasks>

<task type="auto">
  <name>Task 1: Move isHardcodedIgnored into text.ts and remove it from hardcoded.ts</name>
  <files>src/core/scanner/text.ts, src/core/scanner/hardcoded.ts</files>
  <read_first>
    - src/core/scanner/hardcoded.ts — read the full `isHardcodedIgnored` definition (signature, JSDoc, any module-local constants it depends on like a default-ignore regex/list). It must be moved WITH any private helpers/constants it relies on that are not already in text.ts.
    - src/core/scanner/text.ts — confirm what already lives here (stripComments, isStaticStringLiteral, getBaseKey, isKeyUsed, matchWildcard) so the moved function is appended cleanly and exported the same way as its neighbors.
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — "isHardcodedIgnored analysis" and "Common Pitfalls / Pitfall 1".
  </read_first>
  <action>
    1. Copy the complete `isHardcodedIgnored` function (and any module-private constant/helper it
       depends on that does NOT already exist in text.ts — e.g. a punctuation/number/HTML-entity
       ignore pattern) from `src/core/scanner/hardcoded.ts` into `src/core/scanner/text.ts`,
       exported with the same `export function` form used by the other text.ts helpers.
    2. Remove the `isHardcodedIgnored` definition (and any now-orphaned private helper that ONLY
       it used) from `src/core/scanner/hardcoded.ts`. Leave `scanTemplateTextNodes` in
       hardcoded.ts for now — that module is deleted wholesale in Plan 03.
    3. Do NOT yet touch `index.ts` re-exports (`export * from "./text"` already surfaces the moved
       function via the barrel). Do NOT delete hardcoded.ts in this plan.
    Behavior must be byte-identical — this is a move, not a rewrite.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isHardcodedIgnored" src/core/scanner/text.ts` shows the exported definition.
    - `grep -c "function isHardcodedIgnored" src/core/scanner/hardcoded.ts` returns 0 (definition removed from old home).
    - `pnpm tsc --noEmit` exits 0 (validate.ts still resolves isHardcodedIgnored via the @/core/scanner barrel which re-exports text.ts).
  </acceptance_criteria>
  <done>isHardcodedIgnored is defined and exported in text.ts and no longer defined in hardcoded.ts; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint validate.ts and hardcoded.test.ts imports to the new home, then gate</name>
  <files>src/commands/validate.ts, src/__tests__/hardcoded.test.ts</files>
  <read_first>
    - src/commands/validate.ts — the import block at lines ~6-20 imports `isHardcodedIgnored` from "@/core/scanner" (alongside many regex symbols that Plan 03 removes). Confirm where isHardcodedIgnored is used (the AST branch, ~line 314).
    - src/__tests__/hardcoded.test.ts — the `isHardcodedIgnored` describe block currently imports from "@/core/scanner/hardcoded". This import must move to "@/core/scanner/text".
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — File 3 test-disposition map (isHardcodedIgnored REPOINT rows).
  </read_first>
  <action>
    1. In `src/__tests__/hardcoded.test.ts`: change the import of `isHardcodedIgnored` from
       `@/core/scanner/hardcoded` to `@/core/scanner/text`. Leave the `scanTemplateTextNodes`
       import and its describe blocks UNTOUCHED in this plan (they are dropped in Plan 03, after
       Plan 01's GAP coverage is confirmed). If both symbols share one import statement, split it
       so `isHardcodedIgnored` comes from `@/core/scanner/text` and `scanTemplateTextNodes` still
       comes from `@/core/scanner/hardcoded`.
    2. In `src/commands/validate.ts`: leave `isHardcodedIgnored` in the existing `@/core/scanner`
       import for now (it resolves through the barrel re-export of text.ts). Do NOT prune the other
       regex symbols from this import yet — that is Plan 03's job (Pitfall 6). Only act here if the
       isHardcodedIgnored import would otherwise dangle; the barrel keeps it valid, so the expected
       change to validate.ts in THIS plan is none. Confirm via typecheck.
    3. Run the full gate to prove the move + repoint is behavior-preserving end to end.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@/core/scanner/text" src/__tests__/hardcoded.test.ts` shows isHardcodedIgnored imported from text.
    - `grep -n "isHardcodedIgnored" src/__tests__/hardcoded.test.ts | grep -i "hardcoded\"" ` does NOT show isHardcodedIgnored imported from "@/core/scanner/hardcoded".
    - `pnpm tsc --noEmit` exits 0; `pnpm test` exits 0; `pnpm build` exits 0.
    - The `isHardcodedIgnored` describe block in hardcoded.test.ts still passes (5 its) against the new home.
  </acceptance_criteria>
  <done>All isHardcodedIgnored importers resolve from text.ts; the function's tests pass from the new home; the full quality gate is green. hardcoded.ts is now safe to delete in Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none changed) | A function moves between two internal modules. No trust boundary is added, removed, or crossed differently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | isHardcodedIgnored relocation | accept | Pure code move within the package, behavior byte-identical, verified by the function's existing unit tests passing from the new import path. No new input handling or surface. |

No HIGH-severity threats. Relocating an internal helper introduces no new attack surface; the
typecheck + existing unit tests confirm no behavioral drift.
</threat_model>

<verification>
- `pnpm tsc --noEmit && pnpm test && pnpm build` all exit 0.
- isHardcodedIgnored defined in text.ts, absent from hardcoded.ts.
- hardcoded.test.ts imports isHardcodedIgnored from @/core/scanner/text and its describe passes.
- No behavioral change (same function, relocated).
</verification>

<success_criteria>
- `isHardcodedIgnored` is the sole responsibility-mate of the other text.ts helpers, exported from text.ts.
- Every importer (validate.ts via barrel, hardcoded.test.ts directly) resolves correctly.
- The full gate is green, satisfying the carry-forward pre-condition that hardcoded.ts can now be deleted.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup-release/06-02-SUMMARY.md`.
</output>
