---
phase: 06-cleanup-release
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - src/core/scanner/index.ts
  - src/commands/validate.ts
  - src/commands/extract.ts
  - src/commands/prune.ts
  - src/utils.ts
  - src/core/scanner/regex.ts
  - src/core/scanner/dynamic.ts
  - src/core/scanner/hardcoded.ts
  - src/core/scanner.ts
  - src/core/scanner.test.ts
  - src/__tests__/dynamic.test.ts
  - src/__tests__/hardcoded.test.ts
  - src/__tests__/ast-shadow.test.ts
autonomous: true
requirements: [CLEAN-01]
must_haves:
  truths:
    - "detectUsedKeys has no useAst parameter and runs the AST path unconditionally"
    - "detectUsedKeys still returns { usedKeys, fileContents, parsedResults, parseErrors } so looseKeyMatch keeps working"
    - "regex.ts, dynamic.ts, hardcoded.ts, and the scanner.ts shim no longer exist"
    - "No source or test file imports a deleted module (typecheck is clean)"
    - "escapeRegex and isStaticStringLiteral re-exports are gone from utils.ts; utils.ts still exports log + locale-io + surviving text helpers"
    - "Only regex-internal tests are dropped; all shared-function and behavioral tests survive (repointed or de-flagged)"
  artifacts:
    - path: "src/core/scanner/index.ts"
      provides: "AST-only detectUsedKeys, no useAst branch, trimmed barrel re-exports"
      contains: "detectUsedKeys"
    - path: "src/core/scanner/text.ts"
      provides: "surviving shared helpers (now the only scanner helper home besides files/lines)"
      contains: "isHardcodedIgnored"
  key_links:
    - from: "src/commands/validate.ts"
      to: "src/core/scanner/index.ts (detectUsedKeys, AST-only)"
      via: "import without useAst"
      pattern: "detectUsedKeys"
    - from: "src/commands/validate.ts"
      to: "fileContents (looseKeyMatch second pass)"
      via: "detectUsedKeys return value"
      pattern: "fileContents"
---

<objective>
Remove the internal `useAst` flag and delete the dead regex engine. After Plan 02 moved
`isHardcodedIgnored` to text.ts, the regex code is reachable only through the now-removable
`useAst` branch. This plan: (1) strips `useAst` from `detectUsedKeys` and its consumers and makes
the AST path unconditional; (2) deletes `regex.ts`, `dynamic.ts`, `hardcoded.ts`, and the
`scanner.ts` shim, trims the barrel + utils re-exports; (3) performs the surgical test cleanup
(drop regex-internal tests, repoint shared-function tests, delete dynamic.test.ts, de-flag
ast-shadow.test.ts). The typechecker is the deletion verifier throughout.

Purpose: Satisfy ROADMAP success criteria #1 and #2 — modules deleted, flag removed, full suite
green with only regex-internal tests gone (CLEAN-01).

Output: A regex-free scanner whose only path is AST; a clean barrel; a clean utils.ts; a test
suite that proves no behavioral coverage was lost (Plan 01's GAP tests + repointed shared-fn tests
+ de-flagged ast-shadow tests). The quality gate is green.

CRITICAL invariant (carry-forward): `detectUsedKeys` MUST still return `fileContents` so
`looseKeyMatch` keeps working. The ASYNC-03 regression test in scanner.test.ts ("detectUsedKeys"
describe) must stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanup-release/06-RESEARCH.md
@.planning/phases/06-cleanup-release/06-VALIDATION.md

# Production code touched (read before editing each):
@src/core/scanner/index.ts
@src/commands/validate.ts
@src/commands/extract.ts
@src/commands/prune.ts
@src/utils.ts
# Modules being deleted (read to confirm no surviving symbol is lost):
@src/core/scanner/regex.ts
@src/core/scanner/dynamic.ts
@src/core/scanner/hardcoded.ts
@src/core/scanner.ts
# Tests touched (read against the test-disposition map):
@src/core/scanner.test.ts
@src/__tests__/dynamic.test.ts
@src/__tests__/hardcoded.test.ts
@src/__tests__/ast-shadow.test.ts

<interfaces>
<!-- detectUsedKeys AST-only signature after cleanup (from RESEARCH Code Examples). -->
export async function detectUsedKeys(
  files: string[],
  matchFunctions: string[],
  matchAttributes: string[],
  opts?: { cwd?: string; maxConcurrency?: number }   // useAst REMOVED
): Promise<{
  usedKeys: Set<string>
  fileContents: string[]      // MUST stay — looseKeyMatch depends on it
  parsedResults: ParsedFileResult[]
  parseErrors: FileParseError[]
}>

<!-- The four flip sites (RESEARCH Blast Radius), all currently `?? true`: -->
src/core/scanner/index.ts:37  const useAst = opts?.useAst ?? true     → delete line + if/else, keep AST body; also drop the top-of-file `import { buildKeyRegex, buildAttrRegex } from "./regex"`
src/commands/validate.ts:104  const useAst = options?.useAst ?? true  → delete line + the if(!useAst) regex branch (~lines 141-200) + collapse the hardcoded if(!useAst)/else at ~line 270 to the AST body
src/commands/extract.ts:45    { cwd, useAst: options?.useAst ?? true } → drop the useAst property
src/commands/prune.ts:65      { cwd, useAst: options.useAst ?? true }  → drop the useAst property
src/cli.ts                    passes no useAst                          → NO CHANGE

<!-- validate.ts import list to trim (Pitfall 6): remove from the @/core/scanner import -->
buildKeyRegex, buildAttrRegex, buildDynamicCallRegex, isStaticStringLiteral,
classifyDynamicCall, scanTemplateTextNodes
<!-- KEEP in validate.ts import: scanSourceFiles, detectUsedKeys, getBaseKey,
     computeLineOffsets, offsetToLine, matchWildcard, isHardcodedIgnored -->

<!-- Barrel after cleanup (src/core/scanner/index.ts) — remove 3 lines: -->
export * from "./files"
export * from "./regex"      ← DELETE
export * from "./text"
export * from "./dynamic"    ← DELETE
export * from "./lines"
export * from "./hardcoded"  ← DELETE

<!-- utils.ts re-export block: remove `escapeRegex` and `isStaticStringLiteral`,
     keep getFiles, stripComments, getBaseKey, isKeyUsed, matchWildcard,
     keep all locale-io re-exports and the `log` object. -->
</interfaces>
</context>

<project_compliance>
This repo is GitNexus-indexed (see ./CLAUDE.md "Always Do"). For this plan:
- **Impact analysis is already satisfied** by `06-RESEARCH.md`, which mapped the full blast radius of
  every symbol touched here — `detectUsedKeys`, the four `useAst` flip sites (`validate.ts`,
  `extract.ts`, `prune.ts`, `index.ts`), `escapeRegex`/`isStaticStringLiteral` re-export consumers,
  and the deleted modules' callers — from direct reads. Treat RESEARCH as the `gitnexus_impact`
  result; no separate impact run is required before editing these symbols.
- **Before each commit**, run `gitnexus_detect_changes()` to confirm only the expected symbols/files
  changed (CLAUDE.md MUST). If GitNexus reports the index is stale, run `npx gitnexus analyze` first;
  if GitNexus is unavailable, note that in the SUMMARY and proceed — `pnpm tsc --noEmit` (the
  deletion verifier) is the hard correctness backstop.
- Symbols are deleted/relocated, not renamed in place, so `gitnexus_rename` does not apply here.
</project_compliance>

<tasks>

<task type="auto">
  <name>Task 1: Remove the useAst flag from detectUsedKeys and its consumers (AST path unconditional)</name>
  <files>src/core/scanner/index.ts, src/commands/validate.ts, src/commands/extract.ts, src/commands/prune.ts</files>
  <read_first>
    - src/core/scanner/index.ts — lines 1-117: the top imports (`buildKeyRegex, buildAttrRegex` from "./regex"), the `useAst` default at line 37, the `if (useAst) { ... }` AST block (lines ~42-87), and the trailing regex block (lines ~89-115) that must be deleted.
    - src/commands/validate.ts — line 104 (`const useAst`), the `if (!useAst)` regex branch (~141-200), and the second `if (!useAst)` / `else` for hardcoded (~line 270 + the isHardcodedIgnored call ~314).
    - src/commands/extract.ts (line 45) and src/commands/prune.ts (line 65) — the `useAst:` property in the detectUsedKeys opts object.
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — "Symbol-Deletion Blast Radius" (4 flip sites table) and "Code Examples" (the before/after detectUsedKeys).
  </read_first>
  <action>
    Make the AST path the only path. Do NOT delete the regex MODULE files yet (Task 2 does that) —
    here we only remove the flag and the in-place regex branches so nothing calls the modules.

    1. src/core/scanner/index.ts:
       - Delete `const useAst = opts?.useAst ?? true` (line ~37) and remove `useAst?: boolean` from
         the `opts` parameter type — final opts type: `{ cwd?: string; maxConcurrency?: number }`.
       - Unwrap the `if (useAst) { ... }` block so its body runs unconditionally; delete the
         trailing regex path (lines ~89-115, the `buildKeyRegex(...)` / `matchAll` block + its
         `return { usedKeys, fileContents, parsedResults: [], parseErrors: [] }`).
       - Delete the top-of-file `import { buildKeyRegex, buildAttrRegex } from "./regex"`.
       - PRESERVE the AST-path return exactly: `{ usedKeys, fileContents, parsedResults, parseErrors }`.
    2. src/commands/validate.ts:
       - Delete `const useAst = options?.useAst ?? true` (line ~104).
       - Delete the `if (!useAst) { ... }` regex branch (~141-200).
       - Collapse the hardcoded `if (!useAst) { ... } else { ... }` (~270) to just the AST/`else`
         body (the branch that calls `isHardcodedIgnored` at ~314 stays).
       - Trim the `@/core/scanner` import: remove `buildKeyRegex, buildAttrRegex,
         buildDynamicCallRegex, isStaticStringLiteral, classifyDynamicCall, scanTemplateTextNodes`.
         KEEP `scanSourceFiles, detectUsedKeys, getBaseKey, computeLineOffsets, offsetToLine,
         matchWildcard, isHardcodedIgnored`.
    3. src/commands/extract.ts (line 45) and src/commands/prune.ts (line 65): remove the
       `useAst: options?.useAst ?? true` property from the detectUsedKeys opts object (leave `cwd`).
       If `options.useAst` is the only consumer of an options field, leave the surrounding options
       handling intact — just drop the property passed to detectUsedKeys.
    4. src/cli.ts: confirm it passes no useAst (no change expected).
    Do NOT touch utils.ts or the barrel re-exports here (Task 2).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "useAst" src/core src/commands src/cli.ts` returns nothing.
    - `grep -n "buildKeyRegex\|buildAttrRegex" src/core/scanner/index.ts` returns nothing.
    - `grep -n "fileContents" src/core/scanner/index.ts` still shows fileContents in the return.
    - `pnpm tsc --noEmit` exits 0; `pnpm test` exits 0 (ast-shadow.test.ts still passes here because its `useAst: true` calls remain valid — useAst is removed from detectUsedKeys opts, so those calls now error ONLY if the type narrowed; if tsc reports ast-shadow errors, that is expected and is fixed in Task 3 — see note).
  </acceptance_criteria>
  <done>useAst is gone from all production code; detectUsedKeys runs AST-only and still returns fileContents; the regex modules have no in-source caller left. NOTE: if removing useAst from the opts type makes ast-shadow.test.ts's `useAst: true` calls fail tsc, complete Task 3's de-flag in the same pass so the gate is green before commit.</done>
</task>

<task type="auto">
  <name>Task 2: Delete regex.ts, dynamic.ts, hardcoded.ts, the scanner.ts shim; trim barrel + utils re-exports</name>
  <files>src/core/scanner/regex.ts, src/core/scanner/dynamic.ts, src/core/scanner/hardcoded.ts, src/core/scanner.ts, src/core/scanner/index.ts, src/utils.ts</files>
  <read_first>
    - src/core/scanner/regex.ts, src/core/scanner/dynamic.ts, src/core/scanner/hardcoded.ts — confirm no SURVIVING symbol is defined only here (isHardcodedIgnored already moved in Plan 02; scanTemplateTextNodes/classifyDynamicCall/buildKeyRegex/etc. have no surviving caller per RESEARCH).
    - src/core/scanner.ts — the single-line shim (`export * from "./scanner/index"`).
    - src/core/scanner/index.ts — the barrel re-export block (lines 8-13).
    - src/utils.ts — the re-export block (lines 5-19) containing escapeRegex + isStaticStringLiteral.
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — "scanner.ts shim re-exports audit", "What scanner/index.ts re-exports after cleanup", "utils.ts Survival Analysis", "escapeRegex consumers", "isStaticStringLiteral consumers".
  </read_first>
  <action>
    Delete the dead modules and fix the re-export surface. Typecheck after, then run tests.

    1. Delete files: `src/core/scanner/regex.ts`, `src/core/scanner/dynamic.ts`,
       `src/core/scanner/hardcoded.ts`, and `src/core/scanner.ts` (the shim).
    2. src/core/scanner/index.ts: remove the three barrel lines `export * from "./regex"`,
       `export * from "./dynamic"`, `export * from "./hardcoded"`. Keep `./files`, `./text`,
       `./lines`, and the `detectUsedKeys` export.
    3. src/utils.ts: in the `export { ... } from "./core/scanner"` block, remove the
       `escapeRegex` re-export line and the `isStaticStringLiteral` re-export line (both have no
       surviving consumer — VERIFIED in RESEARCH). KEEP `getFiles, stripComments, getBaseKey,
       isKeyUsed, matchWildcard`, the locale-io re-export block, and the `log` object. If the
       source path was `./core/scanner` (directory → index.ts), it still resolves after the shim
       is gone — no path change needed (RESEARCH Pitfall 2). utils.ts SURVIVES.
    4. Note on `@/core/scanner` imports: deleting the shim does NOT break `@/core/scanner` (alias
       resolves to the directory's index.ts). The ONLY relative breakage is `src/core/scanner.test.ts`
       importing `from "./scanner"` — that is repointed in Task 3.
    5. Run `pnpm tsc --noEmit` — it enumerates every remaining dangling import. Resolve each by
       repointing to the correct surviving module (text.ts / index.ts) per the shim re-exports
       audit table; do NOT re-create any deleted symbol.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/core/scanner/regex.ts && test ! -f src/core/scanner/dynamic.ts && test ! -f src/core/scanner/hardcoded.ts && test ! -f src/core/scanner.ts && echo DELETED` prints `DELETED`.
    - `grep -rn "core/scanner/regex\|core/scanner/dynamic\|core/scanner/hardcoded" src/` returns nothing.
    - `grep -n "escapeRegex\|isStaticStringLiteral" src/utils.ts` returns nothing.
    - `grep -n "export \* from \"./regex\"\|export \* from \"./dynamic\"\|export \* from \"./hardcoded\"" src/core/scanner/index.ts` returns nothing.
    - `grep -n "log" src/utils.ts` still shows the log export (utils.ts survives).
    - `pnpm tsc --noEmit` exits 0 (any remaining error means a dangling import was missed — fix it before done). NOTE: test files dynamic.test.ts / scanner.test.ts may still error here; they are fixed in Task 3. If so, finish Task 3 before the final gate.
  </acceptance_criteria>
  <done>The four regex-engine files are gone; the barrel and utils.ts re-export only surviving symbols; production typecheck is clean (test-file fallout resolved in Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Surgical test cleanup — drop regex-internal, repoint shared-fn, delete dynamic.test.ts, de-flag ast-shadow.test.ts</name>
  <files>src/core/scanner.test.ts, src/__tests__/dynamic.test.ts, src/__tests__/hardcoded.test.ts, src/__tests__/ast-shadow.test.ts</files>
  <read_first>
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — THE TEST-DISPOSITION MAP (Files 1-4) is the authority for every drop/repoint/deflag below. Read it in full before editing tests.
    - src/core/scanner.test.ts — imports `from "./scanner"` (the deleted shim). Has describes: stripComments edge cases (REPOINT), getBaseKey+isKeyUsed (REPOINT), matchWildcard (REPOINT), isStaticStringLiteral (DROP 9), regex builders (DROP 5), detectUsedKeys (KEEP 2 incl. the looseKeyMatch/ASYNC-03 regression test).
    - src/__tests__/dynamic.test.ts — imports the deleted dynamic module; RESEARCH confirms all 14 cases covered by typescript.test.ts PARSE-04 parity → DELETE the whole file.
    - src/__tests__/hardcoded.test.ts — drop the scanTemplateTextNodes describe blocks (covered by Plan 01 GAP-01..08 + existing PARSE-05 tests); keep the isHardcodedIgnored describe (already repointed to @/core/scanner/text in Plan 02).
    - src/__tests__/ast-shadow.test.ts — DEFLAG: strip `{ useAst: true }` from all validate/extract/detectUsedKeys calls (Tests A-G); KEEP Test H and Test I unchanged.
    - Verify Plan 01's GAP-01..08 are present and green in typescript.test.ts before dropping scanTemplateTextNodes tests (D-08 gate).
  </read_first>
  <action>
    Apply the test-disposition map exactly (D-08 verify-before-delete; D-09 repoint surviving
    shared-function tests don't delete them; D-10 de-flag ast-shadow keep the tests). This is the
    verify-before-delete payoff — Plan 01 must already be green.

    1. src/core/scanner.test.ts (D-09 — repoint surviving shared-fn tests; drop only regex-internal):
       - Change the import from `"./scanner"` to concrete homes: shared helpers (`stripComments`,
         `getBaseKey`, `isKeyUsed`, `matchWildcard`) from `@/core/scanner/text`; `detectUsedKeys`
         from `@/core/scanner/index` (or `@/core/scanner`).
       - DROP the `isStaticStringLiteral` describe (9 its — no surviving caller).
       - DROP the `regex builders` describe (5 its — buildKeyRegex/buildAttrRegex deleted).
       - KEEP + REPOINT: `stripComments edge cases` (5 its), `getBaseKey + isKeyUsed` (4 its),
         `matchWildcard` (2 its), `detectUsedKeys` (2 its — including the looseKeyMatch ASYNC-03
         regression test, which is CRITICAL to keep green).
    2. DELETE `src/__tests__/dynamic.test.ts` entirely (zero surviving tests; coverage lives in
       typescript.test.ts PARSE-04 parity).
    3. src/__tests__/hardcoded.test.ts: remove ALL `scanTemplateTextNodes` describe blocks
       (including the fast-check property test — robustness-only, no equivalent needed) and remove
       the now-unused `scanTemplateTextNodes` import from `@/core/scanner/hardcoded`. KEEP the
       `isHardcodedIgnored` describe (5 its), which already imports from `@/core/scanner/text`.
    4. src/__tests__/ast-shadow.test.ts (D-10 — de-flag, keep all tests): remove `{ useAst: true }` / the `useAst: true` property
       from every call site in Tests A-G (validate(...), extract(...), detectUsedKeys(...)). Test F
       has two calls (one of which also passes `checkHardcoded: true` — keep checkHardcoded, drop
       only useAst). Test H (structural — useAst absent from src/types.ts) and Test I
       (default-is-AST guard, already passes no useAst) stay UNCHANGED.
    5. Run the FULL gate. The suite must be green with: Plan 01 GAP tests passing, repointed
       shared-fn tests passing, dynamic.test.ts gone, hardcoded scanTemplateTextNodes blocks gone,
       ast-shadow de-flagged and passing.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/__tests__/dynamic.test.ts && echo GONE` prints `GONE`.
    - `grep -rn "useAst" src/__tests__/` returns nothing.
    - `grep -n "scanTemplateTextNodes" src/__tests__/hardcoded.test.ts` returns nothing.
    - `grep -n "isStaticStringLiteral\|buildKeyRegex\|buildAttrRegex" src/core/scanner.test.ts` returns nothing.
    - `grep -rn "from \"./scanner\"" src/core/scanner.test.ts` returns nothing (repointed off the deleted shim).
    - `grep -rn "core/scanner/regex\|core/scanner/dynamic\|core/scanner/hardcoded" src/__tests__ src/core` returns nothing.
    - `pnpm tsc --noEmit` exits 0; `pnpm test` exits 0; `pnpm build` exits 0.
    - The detectUsedKeys looseKeyMatch (ASYNC-03) regression test still passes (confirm it appears in scanner.test.ts and is green).
  </acceptance_criteria>
  <done>Only provably regex-internal tests are dropped; all shared-function and behavioral tests survive (repointed/de-flagged); dynamic.test.ts deleted; ast-shadow de-flagged; the full quality gate is green — ROADMAP criterion #2 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source file → scanner | Untrusted source text is read and parsed. UNCHANGED by this plan: the AST path was already the default; this plan only removes the now-dead regex alternative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | detectUsedKeys input (source files) | accept | Parsing untrusted source is read-only (parser-only TS Compiler API, no eval/execution); this risk is unchanged from prior phases. Deleting the regex branch removes a code path — it reduces, not expands, the surface. |
| T-06-04 | Denial of Service | malformed source file during parse | accept | Already mitigated upstream: parse errors are collected-and-continue (ERR-01) and the bounded-concurrency pool is unchanged. This plan deletes code; it adds no new parsing entry point. |
| T-06-05 | Information Disclosure | removed re-exports (escapeRegex/isStaticStringLiteral) | accept | Removing public-surface re-exports shrinks the API; it cannot leak data. No HIGH severity. |

No HIGH-severity threats. This plan deletes a code path (the regex engine) and trims the export
surface. Net effect on attack surface is a reduction: one fewer parsing implementation and two
fewer public re-exports, with no new input handling introduced. Justification for no HIGH:
nothing new accepts input, executes code, or crosses a privilege boundary.
</threat_model>

<verification>
- `pnpm tsc --noEmit && pnpm test && pnpm build` all exit 0.
- `grep -rn "useAst" src/` returns nothing.
- `grep -rn "core/scanner/regex\|core/scanner/dynamic\|core/scanner/hardcoded" src/` returns nothing.
- The four files (regex.ts, dynamic.ts, hardcoded.ts, scanner.ts) are deleted.
- detectUsedKeys still returns fileContents; the ASYNC-03 looseKeyMatch regression test is green.
- dynamic.test.ts deleted; scanTemplateTextNodes describe blocks removed; ast-shadow de-flagged.
</verification>

<success_criteria>
- The scanner has exactly one engine (AST); the useAst flag is fully removed (CLEAN-01 part 1).
- regex.ts/dynamic.ts/hardcoded.ts/shim deleted; escapeRegex re-export removed; barrel trimmed (CLEAN-01).
- Full suite green with ONLY regex-internal tests dropped; all behavioral + shared-fn coverage survives (ROADMAP #2).
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup-release/06-03-SUMMARY.md`.
</output>
