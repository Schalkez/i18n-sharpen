---
phase: 06-cleanup-release
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - scripts/shadow-compare.ts
  - scripts/bench.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [CLEAN-01]
must_haves:
  truths:
    - "scripts/shadow-compare.ts no longer exists and the shadow package script is gone"
    - "scripts/bench.ts measures only the AST path's absolute timing — no regex comparison, no useAst, no threshold exit"
    - "pnpm bench exits 0 regardless of timing (report-only)"
    - "The CI bench step still runs but is a report, not a gate"
    - "scripts typecheck (tsconfig.scripts.json) passes with the regex engine gone"
  artifacts:
    - path: "scripts/bench.ts"
      provides: "AST-only absolute perf report (warmup + median-of-N, dependency-free)"
      contains: "median"
    - path: ".github/workflows/ci.yml"
      provides: "report-only bench step"
      contains: "pnpm bench"
  key_links:
    - from: "scripts/bench.ts"
      to: "src/core/scanner/index.ts (detectUsedKeys, AST-only)"
      via: "detectUsedKeys call with no useAst"
      pattern: "detectUsedKeys"
---

<objective>
Resolve the fate of Phase 5's transitional gate tooling now that the regex engine is gone.
`shadow-compare.ts` diffed regex-vs-AST and is meaningless with one engine (D-04 — delete it).
`bench.ts` computed a live regex-vs-AST delta, which is impossible without regex (D-05 — repurpose
it to AST-only absolute timing). The CI bench step becomes report-only (D-06) so machine-to-machine
runner variance never flakes the build.

Purpose: Remove dead transitional tooling and keep a soft perf signal without a false-failing gate
(CLEAN-01 housekeeping; honors Phase 5's deferred decision).

Output: `shadow-compare.ts` deleted + its package script removed; `bench.ts` repurposed to
AST-only absolute timing (warmup + median-of-N, no new deps, exits 0); CI bench step renamed to a
report. The scripts typecheck (`tsconfig.scripts.json`) passes — which it now must, since Plan 03
removed `useAst` and any leftover `useAst: false` in these scripts would fail it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanup-release/06-RESEARCH.md

# Files touched (read before editing):
@scripts/bench.ts
@scripts/shadow-compare.ts
@package.json
@.github/workflows/ci.yml

<interfaces>
<!-- bench.ts current structure (RESEARCH "scripts/bench.ts Repurpose" table): -->
lines 1-35   walkDir helper + constants (WARMUP=3, N=10, THRESHOLD_MS=100)  → keep walkDir; DROP THRESHOLD_MS
lines 37-54  timeEngine(files, useAst)                                        → drop useAst param; call detectUsedKeys with no useAst
lines 56-60  median()                                                         → keep
lines 62-104 main(): times regex then AST, computes delta, exits non-zero if delta>THRESHOLD → keep AST timing + report; DROP regex timing, delta, and process.exitCode=1

<!-- package.json scripts (verified): -->
line 27  "shadow": "tsx scripts/shadow-compare.ts"   → REMOVE
line 28  "bench": "tsx scripts/bench.ts"             → KEEP

<!-- .github/workflows/ci.yml (verified): -->
line 40  - name: Benchmark (perf gate)   → rename to "Benchmark (AST perf report)"
line 41    run: pnpm bench               → unchanged (script now self-exits 0)
</interfaces>
</context>

<project_compliance>
This repo is GitNexus-indexed (see ./CLAUDE.md "Always Do"). For this plan:
- **Impact analysis is already satisfied** by `06-RESEARCH.md`, which confirmed `shadow-compare.ts`
  and `bench.ts` are dev-time-only scripts and that `bench.ts`'s `detectUsedKeys` call is the only
  cross-boundary link. Treat RESEARCH as the `gitnexus_impact` result.
- **Before each commit**, run `gitnexus_detect_changes()` to confirm only the expected files changed
  (CLAUDE.md MUST). If the index is stale, run `npx gitnexus analyze` first; if GitNexus is
  unavailable, note it in the SUMMARY and proceed — `pnpm typecheck` (covers `scripts/` via
  `tsconfig.scripts.json`) is the hard correctness backstop.
</project_compliance>

<tasks>

<task type="auto">
  <name>Task 1: Delete shadow-compare.ts + the shadow script; repurpose bench.ts to AST-only absolute timing</name>
  <files>scripts/shadow-compare.ts, scripts/bench.ts, package.json</files>
  <read_first>
    - scripts/shadow-compare.ts — confirm it calls detectUsedKeys with both useAst:false and useAst:true (now impossible) — justifies outright deletion (D-04).
    - scripts/bench.ts — read the full file; identify walkDir, the 50-file deterministic slice, WARMUP/N constants, THRESHOLD_MS, timeEngine(files, useAst), median(), and main()'s regex-vs-AST delta + exit logic.
    - package.json — the scripts block (lines ~23-28): the `shadow` and `bench` entries.
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — "scripts/bench.ts Repurpose (D-05/D-06)" and "scripts/shadow-compare.ts Deletion (D-04)" and "Common Pitfalls / Pitfall 5".
  </read_first>
  <action>
    1. Delete `scripts/shadow-compare.ts`.
    2. In `package.json`, remove the `"shadow": "tsx scripts/shadow-compare.ts"` script entry.
       Keep `"bench": "tsx scripts/bench.ts"`. Do NOT change the version here (that is Plan 05).
    3. Repurpose `scripts/bench.ts` to AST-only absolute timing — dependency-free, report-only:
       - Keep `walkDir`, the deterministic ≤50-file slice selection, `WARMUP` and `N` constants,
         and `median()`.
       - Remove the `THRESHOLD_MS` constant entirely.
       - Change `timeEngine(files, useAst)` to `timeEngine(files)`: remove the `useAst` parameter
         and call `detectUsedKeys(files, matchFunctions, matchAttributes, { cwd })` with NO useAst
         (the AST path is the only path now — passing useAst would fail tsconfig.scripts.json).
       - Rewrite `main()` to: build the slice, run WARMUP warmup iterations, time N iterations,
         compute `median()`, and print a single clear AST-timing report line (e.g.
         `AST median over <N> runs on <fileCount> files: <ms> ms`). Remove the regex timing call,
         the delta computation, and any `process.exitCode = 1` / non-zero exit. The script MUST
         exit 0 unconditionally (D-06).
       Keep the implementation hand-rolled and dependency-free; do NOT add any new import beyond
       what already exists (Node built-ins + the project's detectUsedKeys).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm bench</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f scripts/shadow-compare.ts && echo GONE` prints `GONE`.
    - `grep -n "shadow" package.json` returns nothing (the shadow script entry is removed).
    - `grep -n "bench" package.json` still shows the bench script.
    - `grep -n "useAst\|THRESHOLD_MS\|exitCode" scripts/bench.ts` returns nothing.
    - `grep -n "median" scripts/bench.ts` still shows the median helper retained.
    - `pnpm typecheck` exits 0 (covers scripts via tsconfig.scripts.json).
    - `pnpm bench` exits 0 and prints an AST timing report line.
  </acceptance_criteria>
  <done>shadow-compare.ts + the shadow script are gone; bench.ts measures AST-only absolute timing, is dependency-free, and exits 0 regardless of timing.</done>
</task>

<task type="auto">
  <name>Task 2: Make the CI bench step report-only and gate the full build</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — lines ~40-41: the `Benchmark (perf gate)` step running `pnpm bench`.
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — "CI update (.github/workflows/ci.yml)" snippet and D-06 rationale (runner speed varies; no absolute hard gate).
  </read_first>
  <action>
    In `.github/workflows/ci.yml`, rename the bench step from `Benchmark (perf gate)` to
    `Benchmark (AST perf report)` so its intent (a report, not a gate) is clear. Leave
    `run: pnpm bench` unchanged — the repurposed script now self-exits 0, so the step is naturally
    report-only without any extra `|| true` or `continue-on-error`. Do NOT remove the step (D-06
    keeps the perf signal visible). Make no other CI changes.
    Then run the full quality gate to confirm scripts + CI changes leave everything green.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "AST perf report" .github/workflows/ci.yml` shows the renamed step.
    - `grep -n "perf gate" .github/workflows/ci.yml` returns nothing.
    - `grep -n "pnpm bench" .github/workflows/ci.yml` still shows the bench step present.
    - `pnpm tsc --noEmit` exits 0; `pnpm test` exits 0; `pnpm build` exits 0.
  </acceptance_criteria>
  <done>The CI bench step is a clearly-named report that never fails the build; the full quality gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo scripts | Dev-time only. bench.ts reads repo fixture files and times parsing. No external/untrusted input, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Denial of Service | CI bench step | accept | Making the step report-only removes a flaky build-failure vector (machine-variance false gate). It cannot be exploited; it only reduces false negatives in CI. |
| T-06-07 | Tampering | deleted shadow-compare.ts | accept | Removing a dev-time diff script reduces code surface. No runtime impact. |

No HIGH-severity threats. This plan deletes a dev script and softens a CI step; it introduces no
new input handling, dependency, or runtime path. Net surface change is a reduction.
</threat_model>

<verification>
- `pnpm typecheck && pnpm test && pnpm build` all exit 0.
- `pnpm bench` exits 0 and reports AST timing only.
- shadow-compare.ts deleted; shadow script removed from package.json.
- CI bench step renamed to a report; still present; never fails the build.
</verification>

<success_criteria>
- Transitional gate tooling resolved: shadow-compare deleted (D-04), bench AST-only + report-only (D-05/D-06).
- No leftover useAst/regex reference in scripts (tsconfig.scripts.json passes).
- Full quality gate green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup-release/06-04-SUMMARY.md`.
</output>
