---
phase: 06-cleanup-release
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/parsers/typescript.test.ts
autonomous: true
requirements: [CLEAN-01]
must_haves:
  truths:
    - "The 8 scanTemplateTextNodes behaviors that only lived in hardcoded.test.ts are now asserted against the AST parser"
    - "Multi-attribute hardcoded extraction (placeholder/label/title/alt/aria-label) is proven on the AST path"
    - "Dynamic/complex JSX expressions and HTML comments are proven NOT to produce hardcoded candidates on the AST path"
    - "Nested JSX, brace/comment handling, and comparison-operator attributes are proven on the AST path"
  artifacts:
    - path: "src/__tests__/parsers/typescript.test.ts"
      provides: "GAP-01..GAP-08 behavioral coverage ported from scanTemplateTextNodes"
      contains: "GAP-0"
  key_links:
    - from: "src/__tests__/parsers/typescript.test.ts"
      to: "src/core/scanner/parsers/typescript.ts (parseTypeScriptFile hardcodedCandidates)"
      via: "parse + assert hardcodedCandidates"
      pattern: "hardcodedCandidates"
---

<objective>
Close the 8 coverage gaps (GAP-01..GAP-08) that the research found in `scanTemplateTextNodes`
before any regex-internal test or module is deleted. This is the verify-before-delete gate
(D-08): no regex code may be removed until every behavioral case that `hardcoded.test.ts`'s
`scanTemplateTextNodes` describe blocks asserted is provably covered by the AST parser tests.

Purpose: Guarantee zero behavioral coverage is lost when `hardcoded.ts` is deleted in a later
plan (ROADMAP success criterion #2). This is a Wave 0 gate — every subsequent deletion plan
(Plans 02-05) depends on this plan being green.

Output: 8 new test cases (GAP-01..GAP-08) added to `src/__tests__/parsers/typescript.test.ts`,
all passing against the existing AST parser. NO production code is changed in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanup-release/06-RESEARCH.md
@.planning/phases/06-cleanup-release/06-VALIDATION.md

# The behavioral source-of-truth (existing AST parser tests) — read before adding cases:
@src/__tests__/parsers/typescript.test.ts
# The original behaviors being preserved (what scanTemplateTextNodes asserted):
@src/__tests__/hardcoded.test.ts
# The parser whose hardcodedCandidates output the new tests assert against:
@src/core/scanner/parsers/typescript.ts

<interfaces>
<!-- The shape the AST parser returns; new tests assert against hardcodedCandidates. -->
<!-- Extracted from src/core/scanner/parsers/types.ts — confirm exact field names in the file. -->

ParsedFileResult {
  usedKeys: { key: string; ... }[]
  dynamicCalls: ...[]
  hardcodedCandidates: { text: string; offset: number; ... }[]
}

parseTypeScriptFile is the function under test. The existing describe blocks
"hardcoded text candidates (PARSE-05 parity)" and "SKIP_TAGS (PARSE-05)" show the
exact call convention (how a fixture string is parsed and how candidates are asserted).
Mirror that convention exactly — do NOT invent a new harness.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port GAP-01..GAP-04 (attribute + JSX-expression hardcoded coverage)</name>
  <files>src/__tests__/parsers/typescript.test.ts</files>
  <read_first>
    - src/__tests__/parsers/typescript.test.ts — find the describe "hardcoded text candidates (PARSE-05 parity)" block and copy its exact parse-and-assert convention (helper used, how hardcodedCandidates is read, how text/offset is asserted). Add the new cases inside or adjacent to this describe.
    - src/__tests__/hardcoded.test.ts — read the scanTemplateTextNodes describe to see the exact inputs/expected outputs being preserved (these are the behaviors GAP-01..04 port).
    - src/core/scanner/parsers/typescript.ts — confirm how attribute values and JSX expression string literals land in hardcodedCandidates.
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — the COVERAGE GAPS table (GAP-01..08) gives the exact fixture string for each gap.
  </read_first>
  <behavior>
    - GAP-01: parse `<input placeholder="P" label="L" title="T" alt="A" aria-label="AL" />` → hardcodedCandidates contains all 5 values (P, L, T, A, AL).
    - GAP-02: parse `<input placeholder={t("name")} label={myLabel} />` → hardcodedCandidates is empty (dynamic attribute expressions are excluded).
    - GAP-03: parse `<div>{'Welcome'} and {"Goodbye"} and {`Hello`}</div>` → hardcodedCandidates contains Welcome, Goodbye, Hello (all three JSX expression string literals).
    - GAP-04: parse `<div>{ /* comment */ 'Welcome }' }</div>` → hardcodedCandidates contains the text `Welcome }` (brace/comment inside a JSX string expression handled correctly).
  </behavior>
  <action>
    Add 4 test cases to `src/__tests__/parsers/typescript.test.ts`, each named with its GAP id
    so it is greppable (e.g. `it("GAP-01: extracts all allowlisted attribute values", ...)`).
    Use the EXACT parse-and-assert convention already present in the "hardcoded text candidates
    (PARSE-05 parity)" describe — do not introduce a new helper or import.

    Fixtures (verbatim):
    - GAP-01: `<input placeholder="P" label="L" title="T" alt="A" aria-label="AL" />` — assert the candidate texts include all of: `P`, `L`, `T`, `A`, `AL`.
    - GAP-02: `<input placeholder={t("name")} label={myLabel} />` — assert hardcodedCandidates has length 0 (no static text; both attrs are dynamic expressions).
    - GAP-03: `<div>{'Welcome'} and {"Goodbye"} and {\`Hello\`}</div>` — assert candidate texts include `Welcome`, `Goodbye`, `Hello`.
    - GAP-04: `<div>{ /* comment */ 'Welcome }' }</div>` — assert a candidate with text `Welcome }` exists.

    These are added as test-only changes. Wrap each fixture in a valid component context if the
    existing harness requires it (mirror how sibling PARSE-05 tests wrap their fixtures). Do NOT
    modify any file under src/core/. If a case fails, that is a REAL AST-parser bug to report
    (per RESEARCH Assumption A3) — do NOT weaken the assertion to make it pass; surface it.
  </action>
  <verify>
    <automated>pnpm test src/__tests__/parsers/typescript.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GAP-01\|GAP-02\|GAP-03\|GAP-04" src/__tests__/parsers/typescript.test.ts` returns 4 matching test names.
    - `pnpm test src/__tests__/parsers/typescript.test.ts` exits 0 with the 4 new cases passing.
    - No file outside `src/__tests__/parsers/typescript.test.ts` is modified (`git diff --name-only` lists only that file).
  </acceptance_criteria>
  <done>GAP-01..GAP-04 added and green; attribute allowlist, dynamic-attr exclusion, multi JSX-expression, and brace/comment handling are now asserted on the AST path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port GAP-05..GAP-08 (exclusion + structural JSX coverage) and gate the full suite</name>
  <files>src/__tests__/parsers/typescript.test.ts</files>
  <read_first>
    - src/__tests__/parsers/typescript.test.ts — same describe convention used in Task 1; add the remaining cases consistently.
    - src/__tests__/hardcoded.test.ts — the scanTemplateTextNodes cases for dynamic/complex expressions, HTML comments, comparison operators in attributes, and nested JSX.
    - src/core/scanner/parsers/typescript.ts — confirm how the visitor decides a JSX expression is "static string" vs "dynamic/complex" and how it treats nested JSX children.
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — COVERAGE GAPS table rows GAP-05..GAP-08 for exact fixtures.
  </read_first>
  <behavior>
    - GAP-05: parse `<div>{t("key")}</div><div>{"Hello " + user}</div><div>{1 + 2}</div>` → hardcodedCandidates is empty (call expression, concatenation, numeric — all excluded).
    - GAP-06: parse `<>{/* leading */}<div>Keep Me</div></>` style with an HTML/JS comment region → only `Keep Me` is a candidate (comment region excluded). Use the RESEARCH GAP-06 intent: comment block excluded, adjacent real text kept.
    - GAP-07: parse `<div title={x > 0}>hello</div>` → candidates include `hello` (comparison operator in an attribute expression does not break tag parsing; the dynamic attr yields no candidate but the child text does).
    - GAP-08: parse `<div>{isActive && <span>Hello</span>}</div>` → candidates include `Hello` (nested JSX element inside an expression is still walked).
  </behavior>
  <action>
    Add 4 more test cases to `src/__tests__/parsers/typescript.test.ts`, named `GAP-05`..`GAP-08`,
    using the same convention as Task 1.

    Fixtures (verbatim where given by RESEARCH; adapt the comment fixture to valid JSX since raw
    `<!-- -->` is not valid inside .tsx — use a JS comment region for GAP-06 to express the same
    "comment excluded, real text kept" behavior):
    - GAP-05: `<div>{t("key")}</div><div>{"Hello " + user}</div><div>{1 + 2}</div>` — assert hardcodedCandidates length 0.
    - GAP-06: a fragment containing a comment expression and a sibling `<div>Keep Me</div>` — assert candidate texts include `Keep Me` and do NOT include any comment text.
    - GAP-07: `<div title={x > 0}>hello</div>` — assert candidate texts include `hello`; assert no candidate equal to a stringified comparison.
    - GAP-08: `<div>{isActive && <span>Hello</span>}</div>` — assert candidate texts include `Hello`.

    Test-only changes; do NOT touch src/core/. After adding, run the FULL gate to confirm the
    new tests integrate cleanly and nothing else regressed — this plan is the gate for all later
    deletion plans, so it must leave the whole suite green, not just the one file.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GAP-05\|GAP-06\|GAP-07\|GAP-08" src/__tests__/parsers/typescript.test.ts` returns 4 matching test names.
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm test` exits 0 (full suite green — GAP-01..08 all pass alongside existing tests).
    - `git diff --name-only` lists only `src/__tests__/parsers/typescript.test.ts`.
  </acceptance_criteria>
  <done>All 8 GAP cases (GAP-01..GAP-08) present and green; the full suite passes; the verify-before-delete gate is satisfied and later plans may begin deleting regex code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan only adds test cases. No new input handling, no new code path, no new external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | test fixtures asserting parser output | accept | Tests are dev-time only; they assert behavior of an existing parser. No production code, no runtime input, no privilege. No HIGH-severity threat exists for adding test coverage. |

No HIGH-severity threats. This plan adds test coverage to an existing, already-default AST path
and changes no production behavior, no dependency, and no attack surface. Justification: a coverage
gate cannot introduce an exploitable surface — it only proves existing behavior.
</threat_model>

<verification>
- `pnpm tsc --noEmit` exits 0.
- `pnpm test` exits 0 with GAP-01..GAP-08 passing.
- Only `src/__tests__/parsers/typescript.test.ts` changed.
- If any GAP case fails: STOP — it is a real AST-parser coverage regression (RESEARCH A3). Report it; do not delete the corresponding `scanTemplateTextNodes` test in a later plan, and do not weaken the assertion.
</verification>

<success_criteria>
- 8 named tests GAP-01..GAP-08 exist in `typescript.test.ts` and all pass.
- The full quality gate (`pnpm tsc --noEmit && pnpm test`) is green.
- Behavioral parity with the soon-to-be-deleted `scanTemplateTextNodes` describe blocks is proven on the AST path, satisfying D-08 verify-before-delete and unblocking Plans 02-05.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup-release/06-01-SUMMARY.md`.
</output>
