---
phase: 06-cleanup-release
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - README.md
  - CHANGELOG.md
  - package.json
autonomous: true
requirements: [CLEAN-02]
must_haves:
  truths:
    - "README's Programmatic API examples use the async await form for validate/extract/prune"
    - "README has a 'Migration to 0.4.0' section covering sync→async and optional peer deps"
    - "README install docs list the optional per-framework compiler peer deps"
    - "CHANGELOG has a [0.4.0] BREAKING section documenting async API, optional peer deps + install, and regex→AST engine change"
    - "package.json version is 0.4.0"
    - "An annotated git tag v0.4.0 exists pointing at the release commit"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "0.4.0 BREAKING release notes (CLEAN-02)"
      contains: "## [0.4.0]"
    - path: "package.json"
      provides: "version bump to 0.4.0"
      contains: "\"version\": \"0.4.0\""
    - path: "README.md"
      provides: "async API docs + Migration to 0.4.0 + optional peer dep install notes"
      contains: "Migration to 0.4.0"
  key_links:
    - from: "git tag v0.4.0"
      to: "release commit (version 0.4.0 + CHANGELOG + README)"
      via: "annotated tag"
      pattern: "v0.4.0"
---

<objective>
Ship the release artifacts (CLEAN-02 + D-01/D-02/D-03/D-07). Fix the README so the documented API
matches the new async reality (shipping a breaking release with docs showing the old sync API would
actively mislead — treated as a release blocker), write the BREAKING CHANGELOG, bump the version to
0.4.0, and create the annotated git tag `v0.4.0`. This phase prepares the release fully but stops
short of `npm publish` (D-01 — publish is a deliberate manual/CI step the user takes afterward).

Purpose: Satisfy CLEAN-02 (BREAKING CHANGELOG: async API, optional peer deps + per-framework
install, regex→AST engine change) and the D-01/D-02/D-07 release mechanics.

Output: Updated README (async examples, Migration to 0.4.0, peer-dep install notes), a `[0.4.0]`
BREAKING CHANGELOG section, `package.json` at `0.4.0`, and an annotated `v0.4.0` tag. No publish.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cleanup-release/06-RESEARCH.md

# Files touched (read before editing):
@README.md
@CHANGELOG.md
@package.json

<interfaces>
<!-- README sync→async snippet to fix (RESEARCH "README Edits", lines ~206-224): -->
BEFORE:                                  AFTER:
const results = validate(config, cwd)    const results = await validate(config, cwd)
extract(config, cwd)                     await extract(config, cwd)
const result = prune(config, cwd, {...}) const result = await prune(config, cwd, {...})
// also the try/catch block ~216-224: prune(config) → await prune(config)

<!-- README "Migration from 0.0.x / 0.1.x" section starts ~line 229 -->
<!-- README Installation section ~lines 26-37 -->

<!-- CHANGELOG existing format (RESEARCH "Release Mechanics"): -->
## [0.4.0] - 2026-06-03
### Changed (BREAKING)
- ...
<!-- 0.3.0 entry currently reads "## [0.3.0] - Unreleased" — leave it or set its date per your discretion; do NOT alter older entries' content. -->

<!-- package.json: "version": "0.3.0" (line 3) → "0.4.0" -->
<!-- Tag convention: existing tags are lightweight, but D-01 says ANNOTATED — follow D-01. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README — async Programmatic API, Migration to 0.4.0, optional peer-dep install notes (D-07)</name>
  <files>README.md</files>
  <read_first>
    - README.md lines ~26-37 (Installation), ~189-240 (Programmatic API + Migration sections). Confirm exact current snippet text before editing (RESEARCH cites lines 206-224 verbatim).
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — "README Edits (D-07)" with the exact before/after snippet and the peer-dep install block.
    - .planning/phases/06-cleanup-release/06-CONTEXT.md — D-07 (the three required README edits).
  </read_first>
  <action>
    Make the three D-07 edits:
    1. Programmatic API (§ ~line 206): convert the sync examples to async —
       `const results = await validate(config, process.cwd())`, `await extract(config, process.cwd())`,
       `const result: PruneResult = await prune(config, process.cwd(), { force: true })`. Also update
       the try/catch block (~216-224): `prune(config)` → `await prune(config)` and wrap the example in
       an `async` function context if the surrounding snippet requires top-level await to be valid.
    2. Add a new `## Migration to 0.4.0` section (place it BEFORE the existing
       "Migration from 0.0.x / 0.1.x" section so the newest migration is first). Cover:
       - Public API is now async: callers of `validate`/`extract`/`prune` must `await` the result.
         Include a short before/after `await` code example.
       - New optional peer deps: install the compiler for each framework you scan (list the four).
       - Note that a missing compiler now emits an actionable error naming the exact install command.
    3. Installation section (§ ~lines 26-37): after the basic install commands, add an optional
       peer-dep note:
       ```bash
       # Optional: install the compiler for the frameworks you scan
       pnpm add -D typescript          # .ts/.tsx/.js/.jsx scanning
       pnpm add -D @vue/compiler-sfc   # .vue scanning
       pnpm add -D svelte              # .svelte scanning
       pnpm add -D @astrojs/compiler  # .astro scanning
       ```
    Do NOT alter unrelated README content.
  </action>
  <verify>
    <automated>grep -n "await validate" README.md && grep -n "Migration to 0.4.0" README.md && grep -n "@vue/compiler-sfc" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "await validate\|await extract\|await prune" README.md` shows all three async calls.
    - `grep -n "= validate(config" README.md` returns nothing (no leftover sync call in the examples).
    - `grep -n "Migration to 0.4.0" README.md` shows the new section heading.
    - `grep -n "@vue/compiler-sfc" README.md && grep -n "@astrojs/compiler" README.md` show the peer-dep install notes.
  </acceptance_criteria>
  <done>README documents the async API, has a Migration to 0.4.0 section, and lists optional per-framework peer deps; no sync API example remains.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG 0.4.0 BREAKING section + package.json version bump (D-02/D-03)</name>
  <files>CHANGELOG.md, package.json</files>
  <read_first>
    - CHANGELOG.md — first ~60 lines to match the existing entry format (`## [x.y.z] - date`, `### Changed (BREAKING)`), and confirm where the new 0.4.0 entry goes (at the top, above 0.3.0).
    - package.json — line 3 `"version": "0.3.0"`.
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — "CHANGELOG 0.4.0 Section Format" (the exact three-bullet BREAKING content) and "Release Mechanics".
    - .planning/phases/06-cleanup-release/06-CONTEXT.md — D-03 (the three CLEAN-02 items the section must document).
  </read_first>
  <action>
    1. Add a `## [0.4.0] - 2026-06-03` entry at the TOP of CHANGELOG.md (above the 0.3.0 entry),
       with a `### Changed (BREAKING)` subsection documenting exactly the three CLEAN-02 items:
       - **Async public API** — `validate()`/`extract()`/`prune()` now return `Promise`; all callers
         must `await`. Include the short before/after `await` code example from RESEARCH.
       - **New optional peer dependencies** — framework scanning requires the workspace compiler per
         framework; include the four `pnpm add -D` install commands (typescript, @vue/compiler-sfc,
         svelte, @astrojs/compiler) and note that a missing compiler emits an actionable error.
       - **AST engine** — the regex/state-machine scanner is replaced by per-framework AST parsers
         (TS Compiler API for JS/TS; workspace compilers for Vue/Svelte/Astro); accuracy improves;
         no configuration change required.
       Do NOT modify the content of older CHANGELOG entries.
    2. In package.json, change `"version": "0.3.0"` to `"version": "0.4.0"`.
    Then run the full quality gate.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "## \[0.4.0\]" CHANGELOG.md` shows the new entry.
    - `grep -n "BREAKING" CHANGELOG.md` shows the BREAKING subsection.
    - CHANGELOG 0.4.0 section mentions all three: `await` (async API), `@vue/compiler-sfc` (peer deps), and `AST` (engine change) — verify with `grep -n "await\|@vue/compiler-sfc\|AST" CHANGELOG.md` showing matches within the 0.4.0 section.
    - `grep -n "\"version\": \"0.4.0\"" package.json` shows the bumped version.
    - `grep -n "\"version\": \"0.3.0\"" package.json` returns nothing.
    - `pnpm tsc --noEmit` exits 0; `pnpm test` exits 0; `pnpm build` exits 0.
  </acceptance_criteria>
  <done>CHANGELOG has a complete [0.4.0] BREAKING section covering the three CLEAN-02 items; package.json is at 0.4.0; the full gate is green.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the release and create the annotated v0.4.0 tag (D-01) — do NOT publish</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/06-cleanup-release/06-RESEARCH.md — "Release Mechanics (D-01/D-02)" and Open Question #2 (existing tags are lightweight, but D-01 says annotated — follow D-01).
    - .planning/phases/06-cleanup-release/06-CONTEXT.md — D-01 (prep + tag, do NOT publish).
    - package.json — confirm version reads 0.4.0 and `prepublishOnly` is unchanged before tagging.
  </read_first>
  <action>
    1. Ensure the working tree has the release changes from Task 1 + Task 2 committed (the executor's
       commit flow handles this; the release commit message should be something like
       `release(06): v0.4.0 — AST parser rewrite (BREAKING)`). The commit must include README.md,
       CHANGELOG.md, and package.json.
    2. Create an ANNOTATED git tag on that commit (D-01 — explicit, even though prior tags are
       lightweight):
       `git tag -a v0.4.0 -m "v0.4.0 — AST Parser Rewrite (BREAKING): async public API, optional per-framework compiler peer deps, regex→AST engine"`
    3. Do NOT run `npm publish` and do NOT push the tag — publishing/pushing is the user's deliberate
       follow-up step (D-01; out of scope). `prepublishOnly: pnpm build` already guards the eventual
       publish; leave it untouched.
  </action>
  <verify>
    <automated>git tag -n v0.4.0 && git cat-file -t v0.4.0</automated>
  </verify>
  <acceptance_criteria>
    - `git cat-file -t v0.4.0` prints `tag` (annotated, not `commit` — confirms D-01's annotated tag).
    - `git tag -n v0.4.0` prints the tag with its annotation message.
    - `git describe --tags --abbrev=0` returns `v0.4.0`.
    - `grep -n "\"version\": \"0.4.0\"" package.json` confirms the tagged commit is at 0.4.0.
    - No `npm publish` was run (the phase stops at tagging — confirm by absence of any publish step in the executor's actions).
  </acceptance_criteria>
  <done>The release is committed (README + CHANGELOG + version), an annotated v0.4.0 tag points at it, and no publish/push happened — the user runs the public release deliberately afterward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → npm registry | NOT crossed in this phase. D-01 explicitly stops short of `npm publish`; the tag is local. No artifact is published. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Spoofing | git tag v0.4.0 | accept | A local annotated tag introduces no new attack surface; signing/pushing is deferred to the user's deliberate publish step. No HIGH severity. |
| T-06-09 | Information Disclosure | CHANGELOG / README content | accept | Release notes are public-facing docs; they disclose only intended API/migration info. No secrets. |

No HIGH-severity threats. This plan edits docs/metadata and creates a local tag; it adds no code
path, no input handling, and does not publish. The version bump and tag introduce no new attack
surface. Justification for no HIGH: nothing executable or network-facing changes; publish (the only
boundary-crossing action) is explicitly out of scope per D-01.
</threat_model>

<verification>
- `pnpm tsc --noEmit && pnpm test && pnpm build` all exit 0 (gate run in Task 2).
- README: async examples, Migration to 0.4.0 section, peer-dep install notes present.
- CHANGELOG: [0.4.0] BREAKING section with async API + peer deps + AST engine change.
- package.json version is 0.4.0.
- `git cat-file -t v0.4.0` prints `tag` (annotated); no publish/push performed.
</verification>

<success_criteria>
- CLEAN-02 fully satisfied: BREAKING CHANGELOG documents all three items; README matches the async API.
- D-01/D-02/D-03/D-07 satisfied: version 0.4.0, annotated tag, full README update — no publish.
- The release is prep-complete; the user's only remaining step is the deliberate `npm publish`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup-release/06-05-SUMMARY.md`.
</output>
